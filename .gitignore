# ── Python ──
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# ── 视觉图册本地产物（截图含真实用户数据，不进版本控制） ──
docs/visual-map/

# ── 临时文件 ──
*.log
*.tmp
*.bak
.DS_Store
Thumbs.db
.e2e_*.log

# ── 环境变量 ──
.env
.env.local
.env.production
.env.*.local

# ── IDE/工具 ──
.trae/
.opencode/
.omo/
omo/
.vite/
.codeartsdoer/
.idea/
.vscode/
*.swp
*.swo

# ── 数据目录 ──
data/
!data/characters/
!data/voice_samples/
!data/stickers/

# ── 前端 ──
frontend/dist/
frontend/.vite/
frontend/node_modules/
frontend/test-results/
frontend/playwright-report/
frontend/src-backup-*/
*.tsbuildinfo

# ── 数据库 ──
*.db
*.sqlite3
dump.rdb
*.sqlite3-wal
*.sqlite3-shm

# ── 运行时日志/缓存 ──
logs/
cache/

# ── 审计报告（已过期） ──
audit_reports/

# ── 其他 ──
validation_reports/
.uploads/
.coverage
htmlcov/
.superpowers/
.arts/
.playwright-mcp/
.browser_profile/

# ── 运行时产物 ──
*.rdb
audit_output.json
ACA(3)*/

# ── 部署打包产物（一次性 scp payload，不应进入版本控制） ──
deploy_payload.tar.gz
*_payload.tar.gz

# ── 设计稿/截图 ──
*.docx
createrole-*.png
rolesettings-*.png
rolesettings-check
navigation-design-v10.html
concepts-overview.html

# ── 临时测试文件 ──
run_api_only.py
/0
create-role-after-fix/
# ── UI 快照（二进制 diff，不提交） ──
create-role-snapshot.md
create-role-presets.png

# ── Playwright ──
playwright-report/
test-results/

# ── 用户角色卡数据（保留本地，不进入版本控制） ──
config/characters/

# ── 第三方依赖代码（保留本地，不进入版本控制） ──
third_party/

.env

# Node modules at root
node_modules/

# MCP 索引产物（自动生成，本地使用）
.codebase-memory/
memory-config.json
data/e2e_users.db
intro-full.png

# 参赛报名/个人资料（隐私，不入公开仓库，AGENTS §3 数据隐私）
大创赛报名以及后期发展/
AGENTS.md
