# ── Python ──
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# ── 临时文件 ──
*.log
*.tmp
*.bak
.DS_Store
Thumbs.db
.e2e_*.log

# ── 环境变量 ──
.env
.env.local
.env.production
.env.*.local

# ── IDE/工具 ──
.opencode/
.omo/
omo/
.vite/
.codeartsdoer/
.idea/
.vscode/
*.swp
*.swo

# ── 数据目录 ──
data/
!data/characters/
!data/voice_samples/
!data/stickers/

# ── 前端 ──
frontend/dist/
frontend/.vite/
frontend/node_modules/
frontend/test-results/
frontend/playwright-report/
*.tsbuildinfo

# ── 数据库 ──
*.db
*.sqlite3
dump.rdb
*.sqlite3-wal
*.sqlite3-shm

# ── 运行时日志/缓存 ──
logs/
cache/

# ── 审计报告（已过期） ──
audit_reports/

# ── 其他 ──
validation_reports/
.uploads/
.coverage
htmlcov/
.superpowers/
.arts/
.playwright-mcp/
.browser_profile/

# ── 运行时产物 ──
*.rdb
audit_output.json
ACA(3)*/

# ── 设计稿/截图 ──
*.docx
createrole-*.png
rolesettings-*.png
rolesettings-check
navigation-design-v10.html
concepts-overview.html

# ── 临时测试文件 ──
run_api_only.py
/0
create-role-after-fix/
# ── UI 快照（二进制 diff，不提交） ──
create-role-snapshot.md
create-role-presets.png

# ── Playwright ──
playwright-report/
test-results/

# ── 大赛提交材料（保留本地，不进入版本控制） ──
docs/trae_submission/

# ── 用户角色卡数据（保留本地，不进入版本控制） ──
config/characters/

# ── 第三方依赖代码（保留本地，不进入版本控制） ──
third_party/

# ── Triad Navigation 运行时进度（保留本地，不进入版本控制） ──
.triad-navigation/progress.md

.env
