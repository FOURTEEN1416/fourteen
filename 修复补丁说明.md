# 十四 AI 虚拟伴侣系统 - 前后端对齐修复补丁

## 修复概览

本次修复针对前后端对齐分析报告中发现的 10 个问题，提供了一步到位的优化方案。

| 优先级 | 问题 | 状态 |
|--------|------|------|
| P0 | WebSocket 事件未处理（4个） | ✅ 已修复 |
| P0 | 数据获取模式重复 | ✅ 已修复 |
| P0 | shisi 模块静默失败 | ✅ 已修复 |
| P1 | 语音训练 API 未对接 | ✅ 已修复 |
| P1 | 表情包推荐 GET 参数问题 | ✅ 已修复 |
| P1 | 角色表情分配未对接 | ✅ 已修复 |

---

## 详细修复内容

### 1. WebSocket 事件处理修复 ✅

**问题**：后端广播的 `character_switched`、`emotion_stage_changed`、`affinity_changed`、`sticker_send` 四个事件在前端完全没有处理逻辑。

**修复文件**：
- `frontend/src/types/api.ts` - 扩展 WSIncomingMessage 类型定义
- `frontend/src/store/chatStore.ts` - 添加新的状态字段和 setter
- `frontend/src/hooks/useWebSocket.ts` - 添加 4 个新事件的处理逻辑

**新增状态**：
```typescript
currentCharacterId: string | null
currentCharacterName: string | null
emotionStage: string | null
affinity: number | null
lastSticker: { sticker_id: string; category: string } | null
```

**新增事件处理**：
```typescript
case 'character_switched':
  if (data.data?.character_id && data.data?.name) {
    setCurrentCharacter(data.data.character_id, data.data.name)
  }
  break

case 'emotion_stage_changed':
  if (data.data?.new_stage) {
    setEmotionStage(data.data.new_stage)
  }
  break

case 'affinity_changed':
  if (data.data?.new_value !== undefined) {
    setAffinity(data.data.new_value)
  }
  break

case 'sticker_send':
  if (data.data?.sticker_id && data.data?.category) {
    setLastSticker({ sticker_id: data.data.sticker_id, category: data.data.category })
  }
  break

case 'pong':
  // Heartbeat response, connection is alive
  break
```

---

### 2. 数据获取模式统一 ✅

**问题**：7 组数据同时存在 React Query 和自定义 Hook 两套实现，导致重复请求和缓存不一致。

**修复文件**：
- `frontend/src/hooks/useAPI.ts` - 重构为转发层，保留向后兼容

**修复方式**：
```typescript
// DEPRECATED: This file is kept for backward compatibility.
// Please use hooks from useQueries.ts instead for all new code.

import { useEmotionState, useEmotionTrend, usePersonaProfile, 
         usePersonaEvolutionLog, useMemoryFacts, useHealth } from './useQueries'

export { useEmotionState, useEmotionTrend, usePersonaProfile, 
         usePersonaEvolutionLog, useMemoryFacts, useHealth }
```

**迁移指南**：
- `useEmotionState()` → `import { useEmotionState } from './useQueries'`
- `useEmotionTrend(days)` → `import { useEmotionTrend } from './useQueries'`
- `usePersonaProfile()` → `import { usePersonaProfile, usePersonaEvolutionLog } from './useQueries'`
- `useMemoryFacts()` → `import { useMemoryFacts } from './useQueries'`
- `useHealth()` → `import { useHealth } from './useQueries'`

---

### 3. shisi 模块静默失败修复 ✅

**问题**：shisi 模块初始化失败时仅输出 warning 日志，所有 32 个路由静默不可用。

**修复文件**：
- `api/rest_api.py` - 改进错误处理和状态报告

**修复方式**：
```python
shisi_available = False
shisi_error = None
try:
    from shisi.api.registry import setup_shisi
    shisi_reg = setup_shisi(app, run_migrate=True)
    if orchestrator and hasattr(orchestrator, '_character_manager'):
        orchestrator._character_manager = shisi_reg.character_manager
    shisi_available = True
    logger.info("十四模块已挂载到REST API")
except Exception as e:
    shisi_error = str(e)
    logger.error("十四模块挂载失败: %s", e)
    # Add health check endpoint to report shisi status
    @app.get("/api/shisi/status")
    async def shisi_status():
        return {"available": False, "error": shisi_error}
```

**新增端点**：`GET /api/shisi/status` - 可用于前端检查 shisi 模块可用性

---

### 4. 语音训练 API 对接 ✅

**问题**：后端提供了 4 个语音训练端点，但前端未调用。

**修复文件**：
- `frontend/src/api/shisiClient.ts` - 添加 voiceTraining 模块

**新增 API**：
```typescript
voiceTraining: {
  // POST /api/shisi/voice/training/upload
  upload: (files: File[], modelName = 'default') => Promise<...>
  
  // POST /api/shisi/voice/training/preprocess
  preprocess: (modelName: string) => Promise<...>
  
  // POST /api/shisi/voice/training/train
  train: (modelName: string, epochs = 8, batchSize = 4) => Promise<...>
  
  // GET /api/shisi/voice/training/status
  status: () => Promise<...>
}
```

---

### 5. 表情包推荐参数修复 ✅

**问题**：前端使用 GET 方法带 body 传参，某些 HTTP 客户端/代理可能不支持。

**修复文件**：
- `shisi/api/sticker_routes.py` - 将 GET 改为 POST
- `frontend/src/api/shisiClient.ts` - 同步修改调用方式

**修复方式**：
```python
# 后端：GET → POST
@router.post("/recommend", response_model=ApiResponse)
async def recommend_stickers(req: RecommendRequest):
    ...
```

```typescript
// 前端：client.request({ method: 'GET', ... }) → client.post(...)
recommend: (emotionTags: string[], limit = 5) => 
  client.post(`${BASE}/stickers/recommend`, { emotion_tags: emotionTags, limit }).then(unwrap)
```

---

### 6. 角色表情分配功能对接 ✅

**问题**：后端存在角色表情分配端点，但前端未调用。

**修复文件**：
- `frontend/src/api/shisiClient.ts` - 添加 bindToCharacter 方法

**新增 API**：
```typescript
stickers: {
  // PUT /api/shisi/stickers/characters/{character_id}/stickers
  bindToCharacter: (characterId: string, stickerIds: string[], unlockThreshold = 0) => Promise<...>
}
```

---

## 验证清单

修复完成后，请验证以下功能：

### WebSocket 事件
- [ ] 通过后端触发角色切换，前端能实时更新当前角色
- [ ] 情感阶段变化时，前端状态同步更新
- [ ] 好感度变化时，前端显示更新
- [ ] 收到表情包发送事件时，前端能感知

### 数据获取
- [ ] 情感状态、情感趋势、人设档案、记忆事实等数据正常加载
- [ ] 网络面板中无重复请求

### shisi 模块
- [ ] 访问 `/api/shisi/status` 返回可用状态
- [ ] 故意破坏 shisi 依赖后，能看到明确的错误提示

### 语音训练
- [ ] 能上传训练音频文件
- [ ] 能触发预处理
- [ ] 能启动训练并查看状态

### 表情包
- [ ] 表情包推荐功能正常
- [ ] 能将表情包绑定到特定角色

---

## 后续建议

1. **添加前端 UI**：为语音训练功能添加专门的页面或集成到现有训练页面
2. **添加角色表情分配 UI**：在表情包管理页面中添加角色选择器
3. **引入测试框架**：建议添加 Vitest + Testing Library 进行单元测试
4. **监控 WebSocket 事件**：在生产环境中监控 WebSocket 事件处理是否正常

---

## 文件变更汇总

| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|
| `frontend/src/types/api.ts` | 修改 | 扩展 WSIncomingMessage 类型 |
| `frontend/src/store/chatStore.ts` | 修改 | 添加新状态字段和 setter |
| `frontend/src/hooks/useWebSocket.ts` | 修改 | 添加 4 个 WebSocket 事件处理 |
| `frontend/src/hooks/useAPI.ts` | 重构 | 改为转发层，统一使用 useQueries |
| `api/rest_api.py` | 修改 | 改进 shisi 模块错误处理 |
| `frontend/src/api/shisiClient.ts` | 修改 | 添加 voiceTraining 和 bindToCharacter |
| `shisi/api/sticker_routes.py` | 修改 | GET /recommend → POST /recommend |

---

*修复完成时间：2026-05-24*
