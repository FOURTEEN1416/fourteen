# 三体导航体系 · 唯一的你 项目

> 生成日期：2026-05-28
> 生成工具：QwenPaw 流水线 (startup-calibrator → domain-explorer → decision-analyst → constitution-guardian → loop-executor → evolution-auditor)
> 本次执行人：歆歆（协调者）

---

## 第一部分：地图（8层代码地图）

### L1 · 目录结构

\\\
unique-you/
├── frontend/                    # React + Vite 前端 (port 5173)
│   ├── src/
│   │   ├── api/                 # API 层 (queryClient, axiosInstance, api/)
│   │   ├── components/          # 组件层
│   │   │   ├── common/          # 通用 UI 组件 (Toast, Modal, ErrorBoundary)
│   │   │   ├── layout/          # 布局组件 (Sidebar, MobileNav, Layout)
│   │   │   ├── shared/          # 共享组件 (AnimatedPage, ScrollProgress)
│   │   │   ├── chat/            # 聊天组件 (ChatBubble, ChatInput, MessageList)
│   │   │   ├── storyline/       # 故事线组件 (StorylineEditor, StorylineProgress)
│   │   │   ├── settings/        # 设置子组件
│   │   │   └── workspace/       # 工作区子组件
│   │   ├── hooks/               # 自定义 Hooks
│   │   ├── pages/               # 页面级组件
│   │   │   ├── chat/            # 聊天页面
│   │   │   ├── settings/        # 系统设置页面
│   │   │   └── ...              # 根级页面
│   │   ├── store/               # 状态管理 (Zustand)
│   │   ├── styles/              # 样式文件
│   │   ├── types/               # TypeScript 类型
│   │   └── utils/               # 工具函数
│   ├── v2/                      # 设计稿概念演示 (HTML 原型)
│   └── index.html               # 入口 HTML
├── backend/                     # FastAPI 后端 (port 8000)
│   ├── app/
│   │   ├── api/                 # API 路由模块
│   │   │   ├── v1/              # v1 版本路由
│   │   │   └── v2/              # v2 版本路由（实验性）
│   │   ├── core/                # 核心配置
│   │   ├── models/              # SQLAlchemy 模型
│   │   ├── schemas/             # Pydantic 模式
│   │   ├── services/            # 业务逻辑层
│   │   └── security/            # 安全模块
│   │       ├── ContentSafetyFilter.py
│   │       ├── EncryptionManager.py
│   │       ├── PIIAnonymizer.py
│   │       └── PromptInjectionDetector.py
│   ├── tests/                   # 测试
│   └── requirements.txt
├── v2/                          # 设计稿概念演示 (全局)
├── concepts-overview.html       # 设计概念总览
├── .superpowers/
│   └── brainstorm/              # 设计脑暴原型
├── package.json
├── vite.config.ts
├── tsconfig.json
└── postcss.config.js
\\\

### L2 · 架构视图

\\\
┌─────────────────────────────────────────────────────────────┐
│  frontend (React 18 + Vite + TypeScript + Tailwind)         │
│  Port 5173                                                   │
│                                                              │
│  Route Layer (react-router-dom v6)                           │
│  ├── /wechat           → WeChatPage (lazy)                   │
│  ├── /users            → UsersPage (lazy)                    │
│  ├── /users/:userId/*  → UserWorkspace (layout)              │
│  │   ├── create        → CreateRole                          │
│  │   ├── settings/*    → RoleSettings                        │
│  │   ├── status        → StatusCenter                        │
│  │   └── training      → CloneTrainPage                      │
│  ├── /settings/*       → SystemSettingsLayout (layout)       │
│  │   ├── general       → SettingsGeneral (lazy)              │
│  │   ├── llm           → SettingsLLM (lazy)                  │
│  │   ├── voice         → SettingsVoice (lazy)                │
│  │   ├── security      → SettingsSecurity (lazy)             │
│  │   ├── extensions    → SettingsExtensions (lazy)           │
│  │   └── logs          → SettingsLogs (lazy)                 │
│  └── /storyline        → (未实现 - 待添加)                    │
│                                                              │
│  State Layer (Zustand)                                       │
│  ├── chatStore        → 聊天状态                              │
│  └── authStore        → 认证状态                              │
│                                                              │
│  API Layer (axios + @tanstack/react-query)                   │
│  └── /api/* → backend:8000/v1/*                               │
│                                                              │
│  Component Layer                                              │
│  ├── StorylineEditor (在 CharactersPage 中集成)               │
│  ├── Sidebar + MobileNav                                     │
│  ├── ChatBubble + ChatInput + MessageList                    │
│  └── glass-card 类 (14个文件, 73处引用)                       │
└──────────────────────────────┬──────────────────────────────┘
                               │ HTTP (X-API-Key auth)
                               ▼
┌─────────────────────────────────────────────────────────────┐
│  backend (FastAPI + SQLAlchemy + PostgreSQL)                 │
│  Port 8000                                                   │
│                                                              │
│  API Layer: ~146 endpoints (11 router files)                 │
│  ├── v1/characters     → CRUD + voice + storyline            │
│  ├── v1/auth           → 登录/注册                           │
│  ├── v1/chat           → 对话 + WebSocket                    │
│  ├── v1/knowledge      → 知识库                              │
│  ├── v1/media          → 多媒体                              │
│  ├── v1/memory         → 记忆                                │
│  ├── v1/users          → 用户管理                            │
│  ├── v1/system         → 系统配置/状态                       │
│  ├── v1/storyline      → 故事线 CRUD + 配置 + 进度 + 检测     │
│  └── v2/*              → 实验性路由                           │
│                                                              │
│  Service Layer                                                │
│  ├── MiMo TTS (离线 TTS)                                     │
│  ├── TextToSpeech TTS                                        │
│  └── StorylineService                                        │
│                                                              │
│  Security Layer                                               │
│  ├── ContentSafetyFilter                                     │
│  ├── EncryptionManager                                       │
│  ├── PIIAnonymizer                                           │
│  └── PromptInjectionDetector                                 │
│                                                              │
│  Data Layer (SQLAlchemy + PostgreSQL 15)                     │
│  └── localhost:5432                                           │
└─────────────────────────────────────────────────────────────┘
\\\

### L3 · 模块分区评分

| 模块 | 评分 | 说明 |
|------|------|------|
| **核心架构** | 🟢 绿 | FastAPI + React + PostgreSQL 选型扎实 |
| **数据模型** | 🟢 绿 | SQLAlchemy 模型完整，关系设计合理 |
| **API设计** | 🟢 绿 | REST 风格，版本化路由，参数验证 |
| **安全架构** | 🟢 绿 | 多重安全过滤 + 加密 + 敏感信息脱敏 |
| **前端路由** | 🟡 黄 | 基础路由完整，但缺少故事线独立路由 |
| **页面完整性** | 🟡 黄 | 35个页面组件中21个未注册到路由 |
| **Mock数据** | 🔴 红 | 7个页面含 mock/硬编码数据 (UsersPage最严重) |
| **glss-card类** | 🔴 红 | 73处引用，但无对应的CSS定义，页面布局失效 |
| **标签一致性** | 🔴 红 | Settings sidebar="安全" vs tab="状态" 不一致 |
| **测试覆盖** | 🔴 红 | 无明显单元测试/集成测试 |
| **v2路由鉴权** | 🟡 黄 | v2 路由部分未用 X-API-Key 装饰器 |

### L4 · 配置与环境清单

| 配置项 | 值 | 状态 |
|--------|-----|------|
| Node.js | 通过 D:\node.exe 运行 | ✅ |
| npm | 无，用 bunx 替代 | ✅ |
| Python | 3.12 + pip | ✅ |
| PostgreSQL | 15, localhost:5432, user opencode | ✅ |
| 前端端口 | 5173 (Vite) | ✅ 运行中 |
| 后端端口 | 8000 (FastAPI) | ✅ 运行中 |
| 认证方式 | X-API-Key Header | ✅ |
| 环境变量 | DEEPSEEK_API_KEY, 数据库配置 | ✅ |

---

## 第二部分：指南针（ADR + 原则体系）

### 原则体系（≤7条）

1. **前后端分离原则**：前端只关心 UI 和状态，后端只关心业务逻辑和数据。禁止前端直接查询数据库。
2. **API 版本化原则**：所有 API 必须走 /v1/ 或 /v2/ 前缀，禁止裸路由。
3. **X-API-Key 鉴权原则**：除公开端点外，所有 API 必须验证 X-API-Key。
4. **组件可复用原则**：业务组件放在 components/ 下，页面组件只负责组合。禁止页面级组件写重复逻辑。
5. **设计稿即原则**：v2/ 和 concepts-overview.html 中体现的设计风格和交互模式作为前端实现的唯一参考标准。
6. **Mock 数据零容忍**：任何页面组件禁止包含 mock/硬编码数据，必须从 API 获取或停留在加载状态。
7. **CSS 类名必须可解析**：所有在 JSX 中使用的 CSS 类名必须有对应的定义，禁止使用未定义的类名。

### 种子 ADR 记录

#### ADR-001: 使用 React + Vite + TypeScript 构建前端
- **状态**：✅ 已采纳
- **原因**：Vite 提供快速 HMR，TypeScript 提供类型安全
- **替代方案**：Next.js (过于重量级)、Vue (团队偏好 React)
- **影响**：需 Node.js 运行时，使用 bunx 管理依赖

#### ADR-002: 使用 FastAPI + SQLAlchemy + PostgreSQL
- **状态**：✅ 已采纳
- **原因**：Python 生态成熟，FastAPI 异步性能好，SQLAlchemy ORM 完善
- **替代方案**：Django (过于重量级)、Node/Express (性能不如 FastAPI)
- **影响**：需要 Python 3.12+，PostgreSQL 15

#### ADR-003: 故事线作为独立子路由部署
- **状态**：🔄 建议中
- **原因**：当前 StorylineEditor 被嵌套在 CharactersPage 中作为弹窗/面板，缺少独立的路由入口
- **建议**：添加 /users/:userId/storyline 路由，在 UserWorkspace sidebar 中添加"故事线"tab
- **前提**：需要后端 storyline 端点已完备（目前已有6个端点）

#### ADR-004: 标准化 CSS 类名体系
- **状态**：🔄 建议中
- **原因**：当前 glass-card 类在14个文件73处引用，但 CSS 定义缺失
- **建议**：统一在 styles/ 中定义 glass-card 及相关类，或改用 Tailwind 实现
- **替代方案**：删除所有 glass-card 引用改用 Tailwind 内联类

#### ADR-005: 系统设置标签命名统一
- **状态**：🔄 建议中
- **原因**：sidebar label="安全" vs SystemSettingsLayout tab="状态" 不一致
- **建议**：统一为"安全"，或确认"状态"是更合适的描述后统一

---

## 第三部分：闭环控制（Fitness Functions + Compliance Audit）

### Fitness Functions

| 函数 | 检查项 | 自动 | 触发时机 |
|------|--------|------|----------|
| FF-001 | 所有 API 端点有 X-API-Key 装饰器 | ✅ | 代码审查 |
| FF-002 | 前端无未注册到路由的页面文件 | ❌ 需手动 | 架构审计 |
| FF-003 | CSS 类名不在 JSX 中悬浮引用 | ❌ 需手动 | 代码审查 |
| FF-004 | 后端路由全部在 /v1/ 或 /v2/ 下 | ✅ | CI |
| FF-005 | 无 console.log 在提交代码中 | ✅ | Lint |
| FF-006 | 无 mock 数据在前端页面组件中 | ❌ 需手动 | 代码审查 |
| FF-007 | 组件 props 有 TypeScript 类型定义 | ✅ | TypeScript 编译 |
| FF-008 | v2/ 设计稿不被删除或修改 | ❌ 手动 | 尊重设计原则 |

### Compliance Audit 节奏

| 检查类型 | 频率 | 执行人 |
|----------|------|--------|
| 架构健康检查 | 每周 | evolution-auditor |
| Mock 数据审计 | 每次 PR | loop-executor |
| CSS 类名审计 | 每次 PR | loop-executor |
| API 安全审计 | 每两周 | constitution-guardian |
| 设计稿一致性检查 | 每次功能迭代 | domain-explorer |

---

## 第四部分：问题交付清单

### 优先修复（P0 - 影响功能）

1. **🔴 glss-card 类无定义**：14个文件73处引用 \glass-card\ 类，但 CSS 中无对应定义，导致这些页面布局失效
2. **🔴 UsersPage 硬编码 Mock 数据**：用户列表、角色数据等全部硬编码，无法与后端交互

### 重要修复（P1 - 影响质量）

3. **🟡 7个页面含 Mock 数据**：SettingsVoice, SettingsExtensions, SettingsLogs, SettingsSecurity, CreateRole, CloneTrainPage 均有 mock 数据
4. **🟡 标签不一致**：Settings sidebar="安全" vs SystemSettingsLayout tab 显示"状态"
5. **🟡 21个页面组件未注册路由**：35个页面文件仅14个在 App.tsx 中注册

### 改进项（P2 - 建议）

6. **🟢 Storyline 独立路由**：StorylineEditor 已有完整的类型/hooks/API/组件，只需加路由入口
7. **🟢 v2 后端路由鉴权**：部分 v2 路由缺少 X-API-Key 鉴权装饰器

### 勿动项（设计原则）

8. **📐 v2/ 目录**：设计稿概念演示，体现设计原则，禁止删除
9. **📐 concepts-overview.html**：设计概念总览，禁止删除
10. **📐 brainstorm/ 原型**：设计脑暴历史，禁止删除

---

## 工作交接摘要

### 当前状态
- 前后端均在运行状态
- 后端 ~146 API 端点全部可用
- 前端 35 页面文件，仅 14 个接入路由
- Storyline 功能：后端完整（6端点），前端组件就绪（StorylineEditor），缺路由入口

### 下一步建议
1. **立即解决 glss-card 类 ->** 在 styles/ 中补充定义或用 Tailwind 替代
2. **替换 mock 数据 ->** UsersPage 优先，其他 mock 页面跟进
3. **添加 Storyline 路由 ->** UserWorkspace sidebar + App.tsx 路由
4. **统一标签命名 ->** Settings sidebar / SystemSettingsLayout 对齐
5. **补全路由注册 ->** 审查21个未注册页面的用途，决定是否纳入路由

### 关键路径依赖
- 修复 glss-card → 解封所有页面布局
- 替换 mock 数据 → 依赖后端 API 可用（已确认）
- Storyline 路由 → 依赖后端 6 端点（已就绪） + StorylineEditor 组件（已就绪）
