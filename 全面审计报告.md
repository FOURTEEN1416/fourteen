# 十四 AI 虚拟伴侣系统 - 全面代码审计报告

**审计日期**: 2026-05-24  
**审计范围**: 前端 + 后端完整代码库  
**审计维度**: 代码质量、安全问题、性能、架构设计

---

## 执行摘要

本次审计发现 **1 个严重安全问题**、**5 个高风险问题**、**8 个中风险问题** 和 **3 个低风险问题**。

### 风险等级分布

| 风险等级 | 数量 | 主要类别 |
|---------|------|---------|
| **Critical** | 1 | 代码注入 (eval 使用) |
| **High** | 5 | 命令注入、路径遍历、认证缺失、HTTP 传输、调试暴露 |
| **Medium** | 8 | SQL 注入风险、CORS、限流、敏感存储、密钥硬编码等 |
| **Low** | 3 | XSS 潜在风险、依赖安全、日志脱敏 |

### 代码质量评分

| 维度 | 前端 | 后端 | 说明 |
|------|------|------|------|
| 类型安全 | 6/10 | 5/10 | 过度使用 any，类型注解不完整 |
| 架构设计 | 7/10 | 6/10 | 模块耦合度高，缺乏依赖注入 |
| 可维护性 | 7/10 | 6/10 | 部分组件/函数过大 |
| 性能优化 | 6/10 | 6/10 | 存在内存泄漏和 N+1 查询 |
| 错误处理 | 6/10 | 5/10 | 静默异常处理较多 |
| **综合评分** | **6.4/10** | **5.6/10** | 需要重点改进安全和架构 |

---

## 一、严重安全问题 (Critical)

### 1.1 代码注入漏洞 - eval() 使用

**风险等级**: 🔴 Critical

**位置**:
- `tool_system/builtin/calendar_tool.py` 第 56 行
- `my_character/contextual_behavior.py` 第 36 行

**问题代码**:
```python
# calendar_tool.py
result = eval(expression, {"__builtins__": {}}, self.SAFE_BUILTINS)

# contextual_behavior.py
return bool(eval(self.condition, {"__builtins__": {}}, context_vars))
```

**攻击场景**:
1. 攻击者构造特殊表达式绕过白名单检查
2. 通过修改配置文件注入恶意代码
3. 执行任意 Python 代码

**修复方案**:
```python
# 使用 ast.literal_eval 替代 eval
import ast
result = ast.literal_eval(expression)  # 仅支持基本数据类型

# 或使用安全的数学表达式解析库
import numexpr
result = numexpr.evaluate(expression)
```

**修复优先级**: P0 - 立即修复

---

## 二、高风险问题 (High)

### 2.1 命令注入漏洞

**风险等级**: 🟠 High

**位置**:
- `voice/voice_training.py` 第 97-100 行
- `clone_training/decrypt_source.py` 第 219 行

**问题描述**: 使用 `subprocess` 执行系统命令，参数包含用户可控的文件路径。

**修复方案**:
```python
import re

_SAFE_NAME_RE = re.compile(r'[^\w\-]')
safe_name = _SAFE_NAME_RE.sub("_", user_input)

# 使用参数列表而非字符串拼接
proc = await asyncio.create_subprocess_exec(
    "python", "-m", "train", "--model", safe_name,
    stdout=asyncio.subprocess.PIPE,
    stderr=asyncio.subprocess.PIPE,
)
```

### 2.2 路径遍历漏洞

**风险等级**: 🟠 High

**位置**:
- `api/rest_api.py` 第 1119-1124 行
- `api/clone_manager.py` 第 23-26 行

**问题代码**:
```python
@app.get("/api/files/{filename}")
async def serve_file(filename: str):
    file_path = UPLOAD_DIR / filename  # 没有验证路径
    if not file_path.exists():
        raise HTTPException(404, "文件不存在")
    return FileResponse(file_path)
```

**攻击示例**:
```
GET /api/files/../../../etc/passwd
```

**修复方案**:
```python
from pathlib import Path

@app.get("/api/files/{filename}")
async def serve_file(filename: str):
    # 解析并规范化路径
    file_path = (UPLOAD_DIR / filename).resolve()
    
    # 确保路径在允许目录内
    if not str(file_path).startswith(str(UPLOAD_DIR.resolve())):
        raise HTTPException(403, "访问被拒绝")
    
    if not file_path.exists():
        raise HTTPException(404, "文件不存在")
    
    return FileResponse(file_path)
```

### 2.3 认证与授权问题

**风险等级**: 🟠 High

**问题列表**:
1. API Key 默认禁用 (`API_KEY_ENABLED=false`)
2. CORS 配置过于宽松 (`allow_origins=["*"]`)
3. 敏感端点缺乏认证

**修复方案**:
```python
# 生产环境强制启用认证
_api_key_enabled = os.environ.get("API_KEY_ENABLED", "true").lower() == "true"

# 限制 CORS
allow_origins = ["https://your-domain.com"]

# 为所有敏感端点添加认证
@app.get("/api/sensitive", dependencies=[Security(verify_api_key)])
```

### 2.4 HTTP 传输 (非 HTTPS)

**风险等级**: 🟠 High

**位置**:
- `frontend/src/hooks/useWebSocket.ts` - `ws://` 而非 `wss://`
- `api/rest_api.py` - 默认 HTTP

**修复方案**:
- 生产环境强制使用 HTTPS/WSS
- 配置反向代理 (Nginx) 处理 TLS

### 2.5 调试模式暴露

**风险等级**: 🟠 High

**位置**:
- `config/system.yaml` - `debug: true`
- FastAPI 默认 debug 模式

**风险**: FastAPI 的 debug 模式会暴露详细的错误堆栈。

**修复方案**:
```python
# 生产环境配置
app = FastAPI(debug=False)
```

---

## 三、前端代码质量问题

### 3.1 内存泄漏风险

**严重程度**: 🔴 Critical

**位置**:
- `frontend/src/pages/ChannelsPage.tsx` 第 52-70 行
- `frontend/src/hooks/useWebSocket.ts` 第 36-43 行

**问题描述**:
- 定时器清理不完整
- WebSocket 重连竞态条件
- 组件卸载时可能仍有异步操作在进行

**修复方案**:
```typescript
const mountedRef = useRef(true)

useEffect(() => {
  mountedRef.current = true
  // ... 启动定时器/连接
  
  return () => {
    mountedRef.current = false
    // 清理所有资源
    clearInterval(timer)
    ws.close()
  }
}, [])
```

### 3.2 过度使用 any 类型

**严重程度**: 🟠 High

**统计**: 54 处使用

**影响文件**:
- `UsersPage.tsx` - 多处状态使用 `any[]`
- `FavoritesPage.tsx` - 大量 `any` 类型
- `SettingsPage.tsx` - 配置对象使用 `any`
- `api/client.ts` - API 响应处理使用 `any`

**修复方案**:
```typescript
// 使用更精确的类型
type SettingValue = string | number | boolean

interface Setting {
  id: string
  label: string
  type: 'toggle' | 'select' | 'range'
  value: SettingValue
}
```

### 3.3 缺少错误边界覆盖

**严重程度**: 🟠 High

**位置**: `frontend/src/App.tsx`

**问题**: `Sidebar`、`MobileNav`、`ToastContainer` 位于 `ErrorBoundary` 外部。

**修复方案**:
```tsx
<ErrorBoundary>
  <div className="flex h-screen">
    <Sidebar />
    <main>...</main>
    <MobileNav />
    <ToastContainer />
  </div>
</ErrorBoundary>
```

### 3.4 数据获取模式重复

**严重程度**: 🟠 High

**问题**: 7 组数据同时存在 React Query 和自定义 Hook 两套实现。

**修复方案**:
```typescript
// useAPI.ts 重构为转发层
import { useEmotionState, useEmotionTrend, ... } from './useQueries'
export { useEmotionState, useEmotionTrend, ... }
```

---

## 四、后端代码质量问题

### 4.1 函数复杂度过高

**严重程度**: 🔴 Critical

**位置**: `api/rest_api.py` - `create_api_app` 函数超过 1000 行

**问题**: 包含大量嵌套路由定义，圈复杂度极高。

**修复方案**: 拆分为多个 Blueprint/Router
```
api/
  routes/
    chat.py
    character.py
    training.py
    wechat.py
    clone.py
```

### 4.2 模块耦合度过高

**严重程度**: 🔴 Critical

**位置**: `main.py` - `OptimizedOrchestrator` 类

**问题**: 直接实例化和依赖 15+ 个组件，违反依赖倒置原则。

**修复方案**: 使用依赖注入容器
```python
class OptimizedOrchestrator:
    def __init__(self, container: DIContainer):
        self.components = container.resolve_all()
```

### 4.3 类型注解不完整

**严重程度**: 🟠 High

**统计**: 多处函数参数和返回值缺少类型注解

**影响文件**:
- `api/rest_api.py` - 参数缺少类型注解
- `shisi/api/registry.py` - 使用 `Any` 类型过多

### 4.4 异常处理不当

**严重程度**: 🟠 High

**问题列表**:
1. 捕获过于宽泛的异常
2. 静默异常处理
3. 异步异常仅记录 debug 级别日志

**修复方案**:
```python
# 区分异常类型
if isinstance(exc, ValueError):
    return JSONResponse(status_code=400, content={"detail": str(exc)})

# 记录警告日志而非静默
except Exception as e:
    logger.warning("状态迁移失败: %s", e)
    raise
```

### 4.5 资源泄漏

**严重程度**: 🟠 High

**位置**:
- `shisi/character/store.py` - 数据库连接未正确关闭
- `api/websocket_server.py` - WebSocket 连接泄漏

**修复方案**: 使用上下文管理器
```python
@contextmanager
def db_connection(self):
    conn = self._connect()
    try:
        yield conn
    finally:
        conn.close()
```

---

## 五、中低风险问题

### 5.1 SQL 注入风险 (Medium)

**位置**: `clone_training/decrypt_source.py`

**修复方案**: 使用参数化查询
```python
cursor.execute("SELECT * FROM users WHERE id = ?", (user_id,))
```

### 5.2 请求限流不足 (Medium)

**问题**: SlowAPI 为可选依赖，未安装时无替代限流方案。

**修复方案**: 强制安装 SlowAPI 或实现自定义限流

### 5.3 敏感数据存储 (Medium)

**问题**: 微信凭证明文存储

**修复方案**: 使用系统密钥管理器

### 5.4 XSS 潜在风险 (Low)

**位置**: `third_party/wechat-decrypt/monitor_web.py`

**修复方案**: 对渲染内容进行转义

---

## 六、性能问题

### 6.1 N+1 查询问题

**位置**: `shisi/character/store.py`

**问题**: 循环中逐行解析 JSON

### 6.2 大文件读取

**位置**: `api/rest_api.py` - RAG 文档上传

**问题**: 读取整个文件但只使用部分内容

### 6.3 组件重渲染

**位置**: `frontend/src/components/chat/MessageList.tsx`

**问题**: 虚拟列表 Row 组件重复创建

---

## 七、修复优先级建议

### P0 - 立即修复 (Critical)

1. [ ] 替换所有 `eval()` 使用为安全替代方案
2. [ ] 修复路径遍历漏洞，添加路径规范化检查
3. [ ] 修复内存泄漏风险（定时器、WebSocket）
4. [ ] 生产环境强制启用 API 认证

### P1 - 高优先级 (High)

5. [ ] 加强命令注入防护
6. [ ] 配置 HTTPS/WSS
7. [ ] 关闭调试模式
8. [ ] 统一数据获取模式
9. [ ] 拆分大型函数/组件

### P2 - 中优先级 (Medium)

10. [ ] 完善类型注解
11. [ ] 改进异常处理
12. [ ] 修复资源泄漏
13. [ ] 配置严格的 CORS 策略
14. [ ] 实现强制请求限流

### P3 - 低优先级 (Low)

15. [ ] 加密存储微信凭证
16. [ ] 使用参数化查询
17. [ ] 实施 CSRF 防护
18. [ ] 添加安全响应头

---

## 八、正面发现

### 8.1 安全措施

1. ✅ **内容安全过滤** - 自残/暴力/色情内容检测
2. ✅ **Prompt 注入检测** - 规则匹配 + LLM 检测双重防护
3. ✅ **PII 脱敏** - 手机号、身份证、银行卡检测和脱敏
4. ✅ **人设安全校验** - XSS/注入模式检测
5. ✅ **路径遍历防护** - 基本的非法字符检测

### 8.2 架构优点

1. ✅ **模块化设计** - 功能模块分离清晰
2. ✅ **配置管理** - YAML 配置文件结构良好
3. ✅ **日志系统** - 结构化日志记录
4. ✅ **健康检查** - 统一的健康检查框架

---

## 九、参考文件清单

### 关键安全相关文件
- `api/rest_api.py` - REST API 主入口
- `api/websocket_server.py` - WebSocket 服务
- `api/clone_manager.py` - 克隆数据管理
- `safety/content_safety.py` - 内容安全
- `safety/prompt_injection.py` - 注入检测
- `safety/pii_anonymizer.py` - PII 脱敏
- `tool_system/builtin/calendar_tool.py` - 计算器工具
- `voice/voice_training.py` - 语音训练

### 关键前端文件
- `frontend/src/hooks/useWebSocket.ts`
- `frontend/src/hooks/useAPI.ts`
- `frontend/src/store/chatStore.ts`
- `frontend/src/App.tsx`

### 配置文件
- `config/system.yaml`
- `.env.example`

---

## 十、总结与建议

### 主要问题

1. **安全风险**: 存在代码注入、命令注入、路径遍历等严重安全漏洞
2. **架构问题**: 模块耦合度高，缺乏依赖注入，违反单一职责原则
3. **代码质量**: 类型注解不完整，函数复杂度过高，重复代码较多
4. **性能问题**: 存在内存泄漏、N+1 查询、组件重渲染

### 建议行动

1. **立即行动**: 修复所有 Critical 和 High 级别问题
2. **短期计划**: 重构大型函数/组件，完善类型注解
3. **长期规划**: 引入依赖注入框架，建立自动化安全扫描流程
4. **持续改进**: 建立代码审查机制，定期进行安全审计

---

*报告生成时间: 2026-05-24*  
*审计工具: 多智能体并行代码分析*
