# 注意: 此文件可能不是最新的，请以 pyproject.toml 为权威来源
# 建议使用 pip install -e . 安装依赖

# 唯一的你 - 完整依赖清单
#
# 注意：本文件为兼容旧工具链（pip freeze / 容器构建）保留。
# 依赖管理的权威来源是 pyproject.toml 的 [project.dependencies]。
# 请勿手动编辑本文件；如需新增/升级依赖，请修改 pyproject.toml 后同步本文件，
# 或使用 `pip-compile`、`uv export` 等工具从 pyproject.toml 重新生成。

# ========== 核心依赖 ==========
httpx>=0.27.0
chromadb>=0.4.0
PyYAML>=6.0
APScheduler>=3.10.0
sqlalchemy>=2.0.0
numpy>=1.24.0

# ========== API框架 ==========
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0

# ========== 微信解密依赖 ==========
pycryptodome>=3.19
zstandard>=0.22
psutil>=5.9.0
qrcode[pil]>=8.0

# ========== 可观测性 ==========
structlog>=24.0.0
prometheus-client>=0.20.0

# ========== 安全 ==========
cryptography>=42.0.0
slowapi>=0.1.9

# ========== 搜索和RAG ==========
rank-bm25>=0.2.2
duckduckgo-search>=6.0.0

# ========== 日历工具 ==========
chinese-calendar>=1.9.0
lunarcalendar>=0.0.7

# ========== 语音合成 ==========
edge-tts>=6.1.0

# ========== 角色爬虫 ==========
requests>=2.32.0
beautifulsoup4>=4.12.0
trafilatura>=1.6.0

# ========== 文件监控 ==========
watchfiles>=0.21.0

# ========== 缓存 ==========
redis>=5.0.0

# ========== 测试依赖 ==========
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
httpx>=0.27.0  # 用于测试FastAPI

# ========== 开发工具 ==========
# ruff  # 代码格式化（可选）
# mypy  # 类型检查（可选）
