# 沉浸式网站设计方案 - 完整调研报告

## 📋 项目概述

本方案旨在克隆 Maze (mazehq.com) 和 Immersive Garden (immersive-g.com) 风格的前端设计，融合两种风格的优势，打造具有强烈设计感、丰富动态反馈的现代化网站。

---

## 🎨 设计风格分析

### 1. Maze 风格特征

#### 视觉设计
- **配色方案**: 极简浅色系，纯白 (#FFFFFF) 背景 + 深灰 (#181818) 文字
- **排版风格**: 大号标题，清晰的信息层级
- **数据可视化**: 数字滚动动画、流程图、统计图表
- **留白处理**: 大量留白，营造呼吸感

#### 动画特征
| 动画类型 | 实现方式 | 应用场景 |
|---------|---------|---------|
| 数字滚动 | GSAP/Framer Motion | 统计数据展示 |
| 滚动触发 | ScrollTrigger | 内容渐入 |
| 流程动画 | SVG + GSAP | 产品流程图 |
| 悬停效果 | CSS/Framer Motion | 按钮、卡片 |

#### 技术栈
- React + Next.js
- GSAP + ScrollTrigger
- Framer Motion
- 数据可视化库 (D3.js/Recharts)

---

### 2. Immersive Garden 风格特征

#### 视觉设计
- **配色方案**: 深色/浅色主题切换，奢华感
- **3D元素**: WebGL背景、3D模型展示
- **排版风格**: 艺术化、大字体、创意布局
- **品牌调性**: 高端、奢侈品、艺术感

#### 动画特征
| 动画类型 | 实现方式 | 应用场景 |
|---------|---------|---------|
| WebGL背景 | Three.js/React Three Fiber | 首页背景 |
| 视差滚动 | GSAP ScrollTrigger | 多层滚动 |
| 鼠标跟随 | Custom Hook + Framer Motion | 交互反馈 |
| 页面过渡 | Framer Motion AnimatePresence | 路由切换 |

#### 技术栈
- React + Next.js
- Three.js / React Three Fiber
- GSAP + ScrollTrigger
- GLSL Shaders

---

## 🛠️ 技术架构方案

### 推荐技术栈

```
核心框架: Next.js 14+ (App Router)
语言: TypeScript
样式: Tailwind CSS
动画库:
  - GSAP + ScrollTrigger (复杂滚动动画)
  - Framer Motion (组件动画、页面过渡)
  - Lenis (平滑滚动)
3D图形:
  - React Three Fiber (@react-three/fiber)
  - @react-three/drei (3D辅助组件)
  - Three.js
工具库:
  - clsx + tailwind-merge (类名管理)
  - zustand (状态管理)
```

### 项目结构

```
my-immersive-site/
├── app/
│   ├── sections/           # 页面区块组件
│   │   ├── Hero.tsx
│   │   ├── Features.tsx
│   │   ├── Projects.tsx
│   │   └── Contact.tsx
│   ├── components/         # 可复用组件
│   │   ├── WebGLBackground/
│   │   ├── ScrollAnimation/
│   │   ├── CountUp/
│   │   ├── ParallaxImage/
│   │   └── SmoothScroll/
│   ├── hooks/              # 自定义Hooks
│   │   ├── useMousePosition.ts
│   │   ├── useScrollProgress.ts
│   │   └── useInView.ts
│   ├── lib/                # 工具函数
│   │   └── utils.ts
│   ├── page.tsx            # 主页面
│   ├── layout.tsx          # 根布局
│   └── globals.css         # 全局样式
├── components/ui/          # UI组件库
├── public/                 # 静态资源
│   ├── images/
│   ├── models/             # 3D模型
│   └── fonts/
├── next.config.js
├── tailwind.config.ts
└── package.json
```

---

## 📦 GitHub 资源调研结果

### 核心开源项目

#### 1. React Three Fiber 生态
- **仓库**: `pmndrs/react-three-fiber`
- **Stars**: 28k+
- **描述**: React的3D渲染库
- **用途**: WebGL背景、3D元素
- **示例**: https://docs.pmndrs.react-three-fiber.org/

#### 2. GSAP 动画库
- **仓库**: `greensock/GSAP`
- **Stars**: 18k+
- **描述**: 专业级JavaScript动画库
- **用途**: 滚动触发动画、复杂时间线
- **文档**: https://greensock.com/docs/

#### 3. Lenis 平滑滚动
- **仓库**: `darkroomengineering/lenis`
- **Stars**: 6k+
- **描述**: 丝滑的滚动体验
- **用途**: 替代原生滚动，提供惯性效果

#### 4. Framer Motion
- **仓库**: `framer/motion`
- **Stars**: 22k+
- **描述**: React生产级动画库
- **用途**: 组件动画、手势交互、页面过渡

### 模板项目推荐

#### 1. 3D作品集模板
```bash
# 搜索关键词
github "three.js portfolio" stars:>1000
github "react three fiber portfolio" stars:>500
```

#### 2. 平滑滚动网站模板
```bash
# 搜索关键词
github "lenis smooth scroll" stars:>500
github "gsap scrolltrigger website" stars:>1000
```

#### 3. AI网站克隆工具
- **项目**: `ai-website-cloner-template`
- **功能**: 自动分析并重建网站结构
- **特点**: 支持Claude Code、Cursor、GitHub Copilot

---

## 🎯 核心动画实现方案

### 1. 平滑滚动系统 (Lenis + GSAP)

```typescript
// hooks/useSmoothScroll.ts
import Lenis from 'lenis'
import { useEffect } from 'react'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

export function useSmoothScroll() {
  useEffect(() => {
    const lenis = new Lenis({
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      orientation: 'vertical',
      smoothWheel: true,
    })

    function raf(time: number) {
      lenis.raf(time)
      requestAnimationFrame(raf)
    }

    requestAnimationFrame(raf)

    // 同步GSAP ScrollTrigger
    lenis.on('scroll', ScrollTrigger.update)

    gsap.ticker.add((time) => {
      lenis.raf(time * 1000)
    })

    gsap.ticker.lagSmoothing(0)

    return () => {
      lenis.destroy()
    }
  }, [])
}
```

### 2. WebGL 背景组件

```typescript
// components/WebGLBackground.tsx
'use client'
import { Canvas, useFrame } from '@react-three/fiber'
import { useRef } from 'react'
import * as THREE from 'three'

function AnimatedMesh() {
  const meshRef = useRef<THREE.Mesh>(null)
  
  useFrame((state) => {
    if (meshRef.current) {
      meshRef.current.rotation.x = state.clock.getElapsedTime() * 0.05
      meshRef.current.rotation.y = state.clock.getElapsedTime() * 0.03
    }
  })

  return (
    <mesh ref={meshRef}>
      <sphereGeometry args={[2, 64, 64]} />
      <meshStandardMaterial 
        color="#f5f5f0" 
        wireframe 
        transparent 
        opacity={0.3}
      />
    </mesh>
  )
}

export function WebGLBackground() {
  return (
    <div className="fixed inset-0 -z-10">
      <Canvas camera={{ position: [0, 0, 5] }}>
        <ambientLight intensity={0.5} />
        <pointLight position={[10, 10, 10]} />
        <AnimatedMesh />
      </Canvas>
    </div>
  )
}
```

### 3. 滚动触发动画

```typescript
// components/ScrollReveal.tsx
'use client'
import { useEffect, useRef } from 'react'
import gsap from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

interface ScrollRevealProps {
  children: React.ReactNode
  direction?: 'up' | 'down' | 'left' | 'right'
  delay?: number
}

export function ScrollReveal({ 
  children, 
  direction = 'up',
  delay = 0 
}: ScrollRevealProps) {
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const element = ref.current
    if (!element) return

    const directions = {
      up: { y: 50, x: 0 },
      down: { y: -50, x: 0 },
      left: { x: 50, y: 0 },
      right: { x: -50, y: 0 },
    }

    gsap.fromTo(
      element,
      { 
        opacity: 0, 
        ...directions[direction] 
      },
      {
        opacity: 1,
        x: 0,
        y: 0,
        duration: 1,
        delay,
        scrollTrigger: {
          trigger: element,
          start: 'top 85%',
          toggleActions: 'play none none reverse',
        },
      }
    )

    return () => {
      ScrollTrigger.getAll().forEach(trigger => trigger.kill())
    }
  }, [direction, delay])

  return <div ref={ref}>{children}</div>
}
```

### 4. 数字滚动动画

```typescript
// components/CountUp.tsx
'use client'
import { useEffect, useRef, useState } from 'react'
import { useInView } from 'framer-motion'

interface CountUpProps {
  end: number
  duration?: number
  suffix?: string
  prefix?: string
}

export function CountUp({ 
  end, 
  duration = 2,
  suffix = '',
  prefix = '' 
}: CountUpProps) {
  const [count, setCount] = useState(0)
  const ref = useRef<HTMLSpanElement>(null)
  const isInView = useInView(ref, { once: true })

  useEffect(() => {
    if (!isInView) return

    let startTime: number
    const animate = (currentTime: number) => {
      if (!startTime) startTime = currentTime
      const progress = Math.min((currentTime - startTime) / (duration * 1000), 1)
      
      // 使用easeOutQuart缓动
      const easeOutQuart = 1 - Math.pow(1 - progress, 4)
      setCount(Math.floor(easeOutQuart * end))

      if (progress < 1) {
        requestAnimationFrame(animate)
      }
    }

    requestAnimationFrame(animate)
  }, [isInView, end, duration])

  return (
    <span ref={ref}>
      {prefix}{count.toLocaleString()}{suffix}
    </span>
  )
}
```

### 5. 视差滚动效果

```typescript
// hooks/useParallax.ts
import { useScroll, useTransform, MotionValue } from 'framer-motion'

export function useParallax(speed: number = 0.5): MotionValue<number> {
  const { scrollY } = useScroll()
  const y = useTransform(scrollY, [0, 1000], [0, 500 * speed])
  return y
}

// 使用示例
function ParallaxSection() {
  const y = useParallax(0.3)
  
  return (
    <motion.div style={{ y }}>
      {/* 内容 */}
    </motion.div>
  )
}
```

---

## 📝 页面区块设计

### 1. Hero 区域

```typescript
// app/sections/Hero.tsx
'use client'
import { motion } from 'framer-motion'
import { WebGLBackground } from '../components/WebGLBackground'

export function Hero() {
  return (
    <section className="relative h-screen flex items-center justify-center overflow-hidden">
      <WebGLBackground />
      
      <div className="relative z-10 text-center px-4">
        <motion.h1 
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.2 }}
          className="text-5xl md:text-7xl font-bold text-gray-900 mb-6"
        >
          Agentic AI for
          <br />
          <span className="text-blue-600">Vulnerability Management</span>
        </motion.h1>
        
        <motion.p
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.4 }}
          className="text-xl text-gray-600 max-w-2xl mx-auto mb-8"
        >
          AI Agents that investigate and resolve cloud vulnerabilities
        </motion.p>
        
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.6 }}
          className="flex gap-4 justify-center"
        >
          <button className="px-8 py-3 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition">
            Book Demo
          </button>
          <button className="px-8 py-3 border border-gray-300 rounded-full hover:bg-gray-50 transition">
            Learn More
          </button>
        </motion.div>
      </div>
    </section>
  )
}
```

### 2. 数据统计区域 (Maze风格)

```typescript
// app/sections/Stats.tsx
'use client'
import { ScrollReveal } from '../components/ScrollReveal'
import { CountUp } from '../components/CountUp'

const stats = [
  { value: 40000, suffix: '', label: 'Vulnerabilities published in 2024' },
  { value: 90, suffix: '%', label: 'False positives removed' },
  { value: 34, suffix: '%', label: 'Increase in exploits' },
]

export function Stats() {
  return (
    <section className="py-24 bg-gray-50">
      <div className="max-w-6xl mx-auto px-4">
        <ScrollReveal>
          <h2 className="text-3xl font-bold text-center mb-16">
            Vulnerability backlogs keep growing
          </h2>
        </ScrollReveal>
        
        <div className="grid md:grid-cols-3 gap-8">
          {stats.map((stat, index) => (
            <ScrollReveal key={index} delay={index * 0.1}>
              <div className="text-center p-8 bg-white rounded-2xl shadow-sm">
                <div className="text-5xl font-bold text-blue-600 mb-2">
                  <CountUp end={stat.value} suffix={stat.suffix} />
                </div>
                <p className="text-gray-600">{stat.label}</p>
              </div>
            </ScrollReveal>
          ))}
        </div>
      </div>
    </section>
  )
}
```

### 3. 项目展示区域 (Immersive Garden风格)

```typescript
// app/sections/Projects.tsx
'use client'
import { useRef } from 'react'
import { motion, useScroll, useTransform } from 'framer-motion'
import Image from 'next/image'

const projects = [
  {
    title: 'Louis Vuitton VIA',
    category: 'Web Experience',
    image: '/images/project1.jpg',
  },
  {
    title: 'Cartier Watches',
    category: 'E-Shop',
    image: '/images/project2.jpg',
  },
  // ...
]

export function Projects() {
  const containerRef = useRef<HTMLDivElement>(null)
  const { scrollYProgress } = useScroll({
    target: containerRef,
    offset: ['start end', 'end start'],
  })

  const y = useTransform(scrollYProgress, [0, 1], [0, -100])

  return (
    <section ref={containerRef} className="py-24 bg-black text-white">
      <div className="max-w-7xl mx-auto px-4">
        <motion.h2 
          style={{ y }}
          className="text-4xl font-bold mb-16"
        >
          Selected Works
        </motion.h2>
        
        <div className="grid md:grid-cols-2 gap-8">
          {projects.map((project, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: index * 0.1 }}
              viewport={{ once: true }}
              className="group relative overflow-hidden rounded-lg cursor-pointer"
            >
              <div className="aspect-[4/3] relative">
                <Image
                  src={project.image}
                  alt={project.title}
                  fill
                  className="object-cover transition-transform duration-700 group-hover:scale-110"
                />
                <div className="absolute inset-0 bg-black/50 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                  <div className="text-center">
                    <h3 className="text-2xl font-bold">{project.title}</h3>
                    <p className="text-gray-300">{project.category}</p>
                  </div>
                </div>
              </div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  )
}
```

---

## 🚀 开发计划

### 第一阶段：基础架构 (1-2周)
- [ ] 项目初始化和依赖安装
- [ ] 配置 Tailwind CSS 主题
- [ ] 搭建基础布局组件
- [ ] 实现平滑滚动系统

### 第二阶段：核心组件 (2-3周)
- [ ] WebGL背景组件
- [ ] 滚动触发动画组件
- [ ] 数字滚动动画
- [ ] 视差滚动效果

### 第三阶段：页面开发 (2-3周)
- [ ] Hero区域
- [ ] 数据统计区域
- [ ] 功能特性区域
- [ ] 项目展示区域
- [ ] 联系/CTA区域

### 第四阶段：优化和部署 (1周)
- [ ] 性能优化
- [ ] 响应式适配
- [ ] 动画调优
- [ ] 部署上线

---

## 📊 性能优化建议

### 1. 图片优化
```typescript
// 使用Next.js Image组件
<Image
  src="/image.jpg"
  alt="Description"
  width={800}
  height={600}
  placeholder="blur"
  blurDataURL="data:image/jpeg;base64,..."
/>
```

### 2. 动画性能
- 使用 `transform` 和 `opacity` 进行动画
- 添加 `will-change` 属性
- 使用 `requestAnimationFrame`
- 避免布局抖动

### 3. WebGL优化
- 控制多边形数量
- 使用纹理压缩
- 实现LOD（细节层次）
- 懒加载3D场景

### 4. 代码分割
```typescript
// 动态导入大型组件
const WebGLBackground = dynamic(
  () => import('./components/WebGLBackground'),
  { ssr: false }
)
```

---

## 🎓 学习资源

### 官方文档
1. [Next.js Documentation](https://nextjs.org/docs)
2. [React Three Fiber Docs](https://docs.pmndrs.react-three-fiber.org/)
3. [GSAP Documentation](https://greensock.com/docs/)
4. [Framer Motion Docs](https://www.framer.com/motion/)
5. [Tailwind CSS Docs](https://tailwindcss.com/docs)

### 推荐教程
1. **Three.js Journey** - 付费但值得
2. **GSAP ScrollTrigger Express** - 官方免费教程
3. **Poimandres YouTube** - React Three Fiber教程
4. **DesignCode** - Framer Motion教程

### 灵感网站
1. [Awwwards](https://www.awwwards.com/) - 网站设计灵感
2. [Codrops](https://tympanus.net/codrops/) - 前端技术教程
3. [Three.js Examples](https://threejs.org/examples/) - 3D效果示例

---

## 🔍 GitHub搜索策略总结

### 有效关键词组合
```
# 3D和WebGL
"react three fiber" stars:>1000
"three.js portfolio" stars:>500
"webgl animation" stars:>500

# 动画库
"gsap scrolltrigger" stars:>1000
"framer motion" stars:>2000
"lenis smooth scroll" stars:>500

# 完整模板
"next.js portfolio template" stars:>1000
"creative developer portfolio" stars:>500
"awwwards clone" stars:>100

# 设计系统
"tailwind ui components" stars:>1000
"design system react" stars:>500
```

### Awesome系列资源
- `awesome-web-design`
- `awesome-threejs`
- `awesome-react`
- `awesome-creative-coding`

---

## ✅ 检查清单

### 设计实现
- [ ] 配色方案符合品牌调性
- [ ] 排版层级清晰
- [ ] 留白处理得当
- [ ] 响应式适配完整

### 动画效果
- [ ] 平滑滚动正常工作
- [ ] 滚动触发动画流畅
- [ ] 页面过渡自然
- [ ] 鼠标交互反馈及时

### 性能指标
- [ ] Lighthouse评分 > 90
- [ ] 首屏加载 < 3s
- [ ] 动画帧率 > 60fps
- [ ] 无内存泄漏

### 可访问性
- [ ] 支持键盘导航
- [ ] 支持屏幕阅读器
- [ ] 颜色对比度达标
- [ ] 动画可禁用

---

## 📞 后续支持

如需进一步的技术支持或代码实现，可以：
1. 参考上述GitHub资源
2. 查阅官方文档
3. 在相关社区提问
4. 查看示例代码

---

**文档版本**: 1.0
**更新日期**: 2025年
**作者**: AI Assistant
