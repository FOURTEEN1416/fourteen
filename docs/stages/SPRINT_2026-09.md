# SPRINT_2026-09 — 复赛冲刺 · 代码完善 · 嵌入式预研（阶段真源）

> **路由**：`立项`（产出项目简报）+ `整理开发资料`（真源载体补齐）
> **状态**：`plan` 待确认 ｜ **生成**：2026-09-14 ｜ **任务深度**：`标准任务`（内含 `高风险任务` 子项，未授权不执行）
> **Owner**：本文件是本阶段的**唯一阶段真源**；上游不覆盖 `AGENTS.md`（项目宪法）、`CODE_GRAPH.md`（代码实况）
> **依据**：`AGENTS.md` v1.5 §1.2/§1.3/§3/§6/§8 ｜ sliver-vibe-coding `routes-intake.md` / `project-templates.md` / `routes-feature.md`

---

## 1. 本阶段目标（一句话）

**在 2026-09-16 前交齐云南赛区复赛材料；同时把项目从"文本闭环"向"多模态 + 嵌入式"推进，并补齐可对外举证的证据（软著 / 小论文 / 对标产品数据）。**

---

## 2. 目标用户与场景（承接既有，本阶段不变）

| 项 | 内容 |
|----|------|
| 核心用户 | 18–35 岁独居青年与异乡工作者（孤独感 + 日常情绪压力） |
| 延伸 | 泛兴趣陪伴用户；可平移银发居家陪伴 |
| 核心动作 | 微信扫码 → 生成专属人设角色 → 情感对话 / 语音对话 / 主动关怀 → 情感画像随时间演化 |
| 本阶段新增场景 | **表情通道**（摄像头表情→情绪）与**语音情绪通道**（说话声→情绪），补齐命题任务 1 的多模态要求 |

---

## 3. MVP 核心流（本阶段交付范围）

```
[材料线] 可即交包 → 商业计划书 → 网评PPT → 演示视频 → 佐证材料包
[代码线] 现状对齐 → 工程质量(工具链清零) → 多模态表情/语音情绪通道 → 嵌入式PoC
[证据线] 对标抖音产品数据 → 软著提交 → 小论文成稿
```

---

## 4. 功能边界与非目标

### 4.1 本阶段做（in scope）

| 线 | 内容 |
|----|------|
| 材料 | 命题答题要求点名的三项（**3–5 分钟演示视频 / 源代码或可运行 Demo / 商业计划书**）+ 网评 PPT + 佐证包 |
| 代码 | `mypy`/`ruff` 存量债清零、测试补齐、死代码清理（**工程质量**） |
| 多模态 | 表情识别通道、语音情绪通道（**先调研后定方案**，见 §7） |
| 嵌入式 | **预研 + PoC**（承接 `大创赛报名以及后期发展/03-桌面机器人改造设想-草案.md`），不承诺量产 |
| 证据 | 软著提交（即使当期未批）、小论文一篇成稿、对标产品数据引用 |

### 4.2 非目标（explicit non-goals / must-not-block）

- ❌ 不改技术栈 / 框架 / 目录架构 / 部署形态（改基础需单独授权）
- ❌ 不改数据库 schema / 权限模型 / 支付逻辑
- ❌ 不引入兜底层、兼容 shim、重复 owner、投机抽象
- ❌ **不把任何参赛文档入库 / 入 GitHub / 上服务器**（宪法 §3 三不入）
- ❌ **不写任何未实测的指标**（防乐观完成协议）
- ❌ 不重建 `CODE_GRAPH.md`（宪法：代码实况文档**永不从零重建**，只增量刷新漂移段）
- ❌ 不用 mock 数据冒充实测结果

---

## 5. 确认的演进边界（四档）

| 档 | 内容 |
|----|------|
| **当前交付** | 本文件 §3 三条线 |
| **已确认下阶段能力** | ① 嵌入式方向推进（**用户明确"当代码进一步完善的时候，向嵌入式推进"**）② 多模态表情 + 语音情绪通道 ③ 效果证据实测 |
| **不得阻塞项** | 参赛材料截止（9/16）；三端统一 A/B 档；测试基线不可回归 |
| **推测项（不得影响当前架构）** | 桌面机器人硬件选型（ESP32 / 树莓派）、量产形态、B 端银发机构采购路径 |

---

## 6. 数据与业务对象（本阶段新增视角）

既有核心对象不变（用户 / 角色卡 / 记忆 / 剧情线 / 情绪状态）。
**本阶段新增/触及**：
- **表情特征向量**（来自摄像头，属敏感生物特征）→ Owner：`multimodal/`
- **语音情绪特征**（韵律/能量/基频，属敏感生物特征）→ Owner：`voice/`（**当前仅有 TTS，无 ASR、无情绪识别**，见 §8.2）
- **对标产品数据集**（抖音热门项目公开数据，用于材料论证）→ Owner：`大创赛报名以及后期发展/`（本地，三不入）

⚠️ **隐私红线**：表情与语音均属生物特征，必须**本地处理 + 不落盘原文 + 显式告知**，且不得因新增采集破坏宪法 §3「数据隐私」条。

---

## 7. 初步技术路线判断（**明确非最终选型**）

> `technical-selection.md` 是基础决策的唯一 owner；本节只是本阶段的**方向假设**，必须经调研 + 边界 PoC 后才定型。

| 通道 | 现状 | 方向假设（待验证） |
|------|------|------------------|
| 文本情绪 | ✅ 已有（`emotion_engine.py` 状态机 + LLM 分类器） | 保持 |
| 语音**输出** | ✅ 已有（MiMo TTS / Edge-TTS / 本地，三级降级） | 保持 |
| 语音**输入(ASR)** | ❌ **缺** | 待调研：本地 Whisper 系 / 讯飞 ASR（与命题企业对齐） |
| 语音**情绪识别** | ❌ **缺** | 待调研：声学特征 + 预训练模型 vs 端到端 |
| **表情识别** | ❌ **缺**（`multimodal/` 仅 1 个 `multimodal_processor.py`） | 待调研：本地轻量模型 vs 云端；**必须本地优先**（隐私） |

**调研强制约束**：属**代码/技术类任务** → 依宪法 §1.3 + §6，**必须 GitHub-First（`github-search-strategy`），禁止调用 WebSearch**；学术面走 arXiv / Semantic Scholar 等一手源。

---

## 8. 现状探明（只读实测，2026-09-14）

### 8.1 Owner Map 真实性核验 ✅

宪法 §2 列出的 **24 个模块全部真实存在**（`main.py` / `api` / `orchestrator` / `character_card` / `my_character` / `persona_extractor` / `llm_provider` / `memory_ext` / `context` / `multimodal` / `proactive` / `voice` / `plugins` / `clone_training` / `frontend` / `deploy` / `observability` / `security` / `tests` / `tools` / `scripts` / `utils` / `shisi` / `wechat_direct`）。**Owner Map 无失真。**

### 8.2 新增能力缺口（**本次探明的关键发现**）

| 侧 | 现状 | 缺口 |
|----|------|------|
| `voice/` | `audio_converter` / `clone_data_manager` / `mimo_tts_provider` / `tts_manager` / `tts_provider_base` → **全是 TTS（输出）** | **无 ASR（语音输入）**、**无语音情绪识别** |
| `multimodal/` | 仅 `multimodal_processor.py` 一个实现 | **无表情/人脸识别实现** |

→ 命题任务 1「融合语音情绪分析、面部表情识别、文本情感理解及行为模式追踪」中，**语音情绪与表情两条通道确实是空的**。此前方案把它写成"扩展路线"是如实的，但**现在要真正补上**。

### 8.3 ⚠️ 指标口径差异（**不可据此断言 CODE_GRAPH 失真**）

| 指标 | `CODE_GRAPH.md` 声明 | 我的 grep 口径 | 差异原因 |
|------|---------------------|--------------|---------|
| API 端点 | 206（方案）/ 199（CODE_GRAPH v1.3） | 175 | 我只数了 `@(app\|router).(get\|post…)` 装饰器；CODE_GRAPH 用 **运行时 `create_api_app` 实扫**，口径更全（含 `add_api_route` / 子路由挂载） |
| Python 测试 | 1042 | 806 | 我数 `def test_`，未计参数化展开与继承用例 |
| 前端测试 | 75 | 17 | 我数**文件数**，声明是**用例数** |

**结论：口径不同，不是漂移。** 按宪法 §1.3 真值裁决（代码实况 > 现行文档），**复测必须沿用 CODE_GRAPH 的同一测量方法**（`create_api_app` 实扫 + `pytest --collect-only` + `vitest list`），否则就是拿错尺子量。**本项标记 `未验证`，待对齐口径后复测。**

### 8.4 真源载体核验（**含我自己的两处误判纠正**）

| 载体 | 状态 | 说明 |
|------|------|------|
| `docs/README.md` | ✔ 在位 | 文档体系唯一入口，含准入规则（新文档必须登记） |
| `docs/FUNCTION_INVENTORY.md` | ✔ 14814B | 商讨协议第 1 步"定位"坐标系，可用 |
| `docs/VISION.md` / `docs/DECISION_LEDGER.md` / `docs/history/INDEX.md` | ✔ 在位（据 docs/README 真源四件套） | — |
| `docs/P1_BACKLOG.md` | ✔ **在位** | ⚠️ **我先前误判为"不存在"——查错了路径**（它在 `docs/` 下，我查的是项目根） |
| `docs/FEATURE_MAP.md` | **已按用户裁决删除** | ⚠️ **我先前误判为"缺失待补"**。真相：`docs/README.md` 明确记载「已删除（08-28 用户裁决：严重错误）| 由 FUNCTION_INVENTORY.md 替代」。**不是缺口，是已处置** |
| `docs/board/BOARD.md` | ✘→**本阶段已补建** | 宪法 §8 跨窗看板；用户并行多智能体，**留痕必需** |
| `docs/board/TASK_PACKAGES.md` | ✘→**本阶段已补建** | 窗口级任务包切分 |
| `docs/stages/SPRINT_2026-09.md` | **本阶段新建** | 本文件 |

#### ⚠️ 由此暴露的真实漂移：**宪法自身过期**

`AGENTS.md` §1.3 仍写着「新增真值载体 `docs/FEATURE_MAP.md`（功能现状地图,商讨坐标系）」，
但该文件**已于 2026-08-28 由用户裁决删除**（`docs/README.md` §五 明确登记，理由"严重错误"，由 `FUNCTION_INVENTORY.md` 替代）。

→ **这是 `AGENTS.md` 与 `docs/README.md` 冲突**（均为"现行文档"层）。按宪法 §1.3 真值裁决（代码实况 > **现行文档** > 历史文档）与 §6 防漂移规则：
**建议修订 `AGENTS.md` §1.3，把 FEATURE_MAP.md 的引用改为 `FUNCTION_INVENTORY.md`。**
⚠️ **但 AGENTS.md 是项目宪法**，修订需用户确认（`整理开发资料` 环节一并处理）。

#### 我的探明口径教训（记录在案）

本节初稿出现两处错判（P1_BACKLOG 查错路径、FEATURE_MAP 把"已删除"当"待补建"）。
**根因：只探了物理存在性，没查"为什么不存在"。** 按宪法 §1.3 真值裁决第三层（历史文档/登记簿）本应先读 `docs/README.md` —— 它第 52 行就写着答案。
**教训：判断"缺失"之前，先查登记簿有没有写"已删除"。**

### 8.5 Git 状态

`main` 分支，远程 `github.com/FOURTEEN1416/fourteen.git`。**未跟踪 2 项**：`.zcode/`、`frontend/audit-tabs.mjs`(2026-09-03, 2301B) —— 性质未核，**git 状态非全净**，多窗口开工前应处置。

---

## 9. 对标产品与差异化

| 项 | 内容 |
|----|------|
| 用户指定 | **用户将提供一个"和我差不多的抖音热门项目"作为对标**，用于书写缺数据（财务/运营） |
| 待确认 | 该对标的**名称与链接**（`待确认`） |
| 使用红线 | ⚠️ **对标只可用于"区间测算 + 方法来源标注"，不得直接把他人经营数据写成我方数据**（宪法 §3 真实 + 防乐观完成） |
| 既有差异化 | 原生心理分析层 / 三层记忆 + 人格演化 / 可解释主动关怀 / 微信原生触达 / 接近商用工程完成度 |

---

## 10. 后续里程碑

| 时点 | 里程碑 | 验证 |
|------|--------|------|
| 9/15–9/16 | 材料可即交包到位；A0 口径拿到 | 三处逐字一致；《赛事指南》在手或口径记录 |
| 9/16 | **省赛材料提交（学校主体）** | 提交回执 / 学校确认 |
| 9/17–9/24 | 网评材料定型（PPT/BP/视频/佐证） | 对照《评审要点》100 分逐项 + 成片时长核 |
| 9 月下旬 | 省级复赛网评 | 结果公布 |
| 10 月中旬 | 省级决赛（昆明医科大学，现场路演） | 现场答辩 |
| 本阶段内 | 工具链清零 + 多模态通道 PoC + 软著提交 + 小论文成稿 | 见 §11 |

---

## 11. 验收规则

| 产物 | 完成标准 | 验证方法 |
|------|---------|---------|
| 工程质量 | `mypy` / `ruff` 0 错误；测试基线**不回归** | `mypy .`、`ruff check`、`pytest`、`cd frontend && npm test`（宪法 §5） |
| 多模态通道 | 单通道端到端可跑，输出接进既有情感画像 | 真实命令跑通 + 接口回读 |
| 嵌入式 PoC | 最小可演示链路跑通 | 实物/日志证据 |
| 软著 | **提交回执**（不要求当期获批） | 提交编号 |
| 小论文 | 成稿 + 可投稿（走"最快最容易通过"路径） | 成稿 + 目标期刊/会议清单 |
| 材料 | 三处逐字一致 + 规格符合《赛事指南》 | 逐字比对 |

⚠️ **任何一项无新鲜验证证据，不得声称完成**（宪法 §1.2 + 防乐观完成协议）。

---

## 12. 开放问题（**已按用户批评重做：能调研的不再问用户**）

> 用户批评：「**这种问题不应该来问我**」「**我没有交代过不要直接给我问题吗？？要你先搜索调研介绍利弊给选项**」
> **批评成立。** 以下问题已由调研解决（见 `大创赛报名以及后期发展/10-调研简报-媒体上传与论文落点与机器人架构.md`）：
> 论文落点（已给 A/B/C 三档 + 推荐）、机器人算力放置（已给三方案 + 推荐端云分工）、微信媒体上传（已给三路线 + 推荐）、多模态通道方向（已给扩展路径）。

| # | 问题 | 状态 |
|---|------|------|
| Q1 | 对标抖音项目名称/链接 | ⏳ **用户提供中**（"我等下去找"） |
| Q2 | 小论文目标期刊/会议 | ✅ **调研已解决** — A《心理学报》"人工智能基础"专栏 / B CSIG 情感智能大会 / C 开放获取期刊（推荐先 C 后 B）；**仅剩一项属用户裁量**：用参赛内容发表是否受学校规定限制 |
| Q3 | 嵌入式目标形态 | ✅ **调研已解决** — 推荐**端云分工**（机器人=前端端点，程序跑云服务器，即小智范式），复用现有 `api/`+`emotion_engine`+`memory_ext`；扩展 `api/websocket_server.py` 为流式 |
| Q4 | 表情/语音采集合规边界 | ✅ **调研已解决方向** — 推荐"端侧只出特征、不上传原图"以护隐私；路线与红线见调研简报 §3/§4。**产品内是否启用采集仍属你的产品决策** |
| Q5 | 软著申请主体 | ✅ **已定：个人** |
| Q6 | 多智能体窗口分工 | ⛔ **撤回** — 用户批评「不要着急着分发任务，先将要弄什么东西确定了」。切分时机未到，待功能范围确认后重做 |

### 12.1 由此暴露的流程问题（我的）

1. **该调研却去问用户** → 已改为"先调研、给利弊、给推荐"
2. **没确定做什么就分发任务** → `TASK_PACKAGES.md` 已标 **暂缓未生效**
3. **没扫完整个仓库就下结论** → 已完成全量扫描（153 md / 367 py / 234 js / 187 ts / 24 个 `.old`），并发现 **Owner Map 漏项**（`user_scheduler.py` 等）

---

## 13. 下一步（**不自动进入**）

1. **`整理开发资料`**：补建 `docs/FEATURE_MAP.md`、`docs/board/BOARD.md`、`docs/board/TASK_PACKAGES.md`，处置 2 项未跟踪文件
2. **多模态调研**（GitHub-First，禁 WebSearch）
3. **对齐需求**（商讨协议五步制）
4. 才进入 `开发执行`

> 本文件为 `plan` 状态，**未获确认前不实现任何代码**（`routes-feature.md`：Do not implement yet）。


## 14. 执行增量记录（2026-09-17 追加；plan 状态待用户整体确认，本节只记已发生事实）

> 本文件主体仍是 `plan` 状态；以下为阶段内已实际发生的执行事实（商讨协议五步制逐项获用户确认后执行），不改变其余章节。

- **生产体验三连修**（`10c8f0f`/`5e4ecb5`）：web 切角色→微信实时生效（activate↔wechat_bindings 接线，修复两条线断裂）；emoji 收口（默认每条≤1 仅情绪强烈，五处提示词语义化）；主动消息打通（asyncio.run 直投 + 紧迫度回退 ASE 自身 last_chat + MultiProviderGateway 补 chat_sync）——生产日志实证 64 触发 0 送达 → 1/1 "微信主动发送成功"。
- **web 控制端两开关**（`8a34b23`/`4f6ed29`，用户裁决）：免打扰时段滑条（MessageTab）+ 知识库定期采集 Toggle（DATA tab，+`/api/knowledge/collect-config` GET/POST，端点 206→208）；`data/scheduler_config.json` 为跨 worker 调度配置真源（4 worker 仅 master 持调度器）。
- **死代码清洗**（用户裁决"直接清洗"）：shared/Badge、chatStore+api/chat（侧栏连接圆点改接 useWechatStatus 真源）、shisi 微信指令系统——DELETION_LOG 09-17 条。
- **角色卡同步**：本地 53 张去重 29 → 24 张唯一卡入服务器 config/characters（25 含绑定卡）。
- **验证**：pytest 1014 收集/1010 通过/4 跳过 + vitest 87/87 + tsc 0 错；remote_deploy 全流程部署（含服务端前端构建）+ health 200 + 端点远端实测。详见 `docs/verification/2026-09-17-三连修复验证报告.md`。
- §8.2 所探明的部分缺口随本轮收窄：微信↔角色绑定链路（原断裂）已接通；Vault 定时采集（原死接线）已以控制台开关形态接入。
