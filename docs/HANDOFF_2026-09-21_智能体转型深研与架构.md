# HANDOFF — 智能体转型深研与架构升级（下一窗口）

> **文档类型**：跨窗口交接任务书（本窗口**只交接、不实施大改造**）  
> **日期**：2026-09-21  
> **发起**：用户裁决 —「重大决策转型」；对标学习改造**效果很差**；小凌架构学习**未落到实处**；下一窗口必须：调研 → **重新拉仓逐行学实现** → 提出改造方案/框架/技术选型  
> **实施窗口**：新窗口（建议名 `agent-x` / 任务包 **AX**）  
> **主控窗口**：`D:\Desktop\ai-girlfriend`（协调 + 收编 + 真源文档）  
> **宪法**：`AGENTS.md` v1.33 + `docs/board/BOARD.md` + 本文件  
> **性质**：**重大决策转型** — 不是修 bug 批次，是产品架构级重构窗口

---

## 0. 用户原话与裁决（范围确认，不得曲解）

1. **调研**
2. **拉取克隆进行学习**
3. **学习规则**：不是看一个项目「有什么」，更要看**其如何具体实现这个功能**；**读代码不能只读函数名和注释**
4. **提出改造方案、搭建框架、技术选型**
5. **这是一次重大决策转型**
6. **对于「小凌」架构的学习是不是根本没有落到实处** → **是。只有研究报告，无生产代码落地**
7. **对于上传对标学习的改造结果，我认为效果很差** → **接受该判定**；下一窗口禁止再「报告堆叠 → 零散打补丁」模式

**负面边界**
- 禁止把研究文档改写一遍就当「学习成果」
- 禁止只读 README / 函数名 / docstring 写对标表
- 禁止在未给出**可评审的架构选型与迁移路径**前直接大改生产热路径
- 参赛材料目录三不入；不引入 AGPL 传染性代码进主仓
- 主检出不直接实施；一律 worktree

---

## 1. 为什么上一轮「对标改造」效果差（失败模式清单）

下一窗口必须**显式规避**这些失败模式：

| # | 失败模式 | 实证 |
|---|----------|------|
| F1 | **只学「有什么」** | 报告大量模块对照表，未回答「nana 的 memory_extract 如何解析 JSON、失败如何降级、写库事务边界在哪」 |
| F2 | **研究→实施断层** | A3 用户画像槽、C1 结构化回写、F 归档 curator、小凌 EventLedger P0 **均未在生产落地**（画像槽/智能体工具为 09-21 补丁，仍浅） |
| F3 | **碎片补丁代替架构** | 隔离/提醒/prompt/画像连打多批，仍「串台感/画像乱/玩具感」——缺**统一状态真源与因果主轴** |
| F4 | **对照仓丢失** | `D:\Desktop\peer-projects\` 本机已不在，无法复核「当时到底读了哪几行」 |
| F5 | **小凌只停在报告** | `docs/reports/2026-09-19_经历因果升级机制研究.md` 等给出 EventLedger/识海/心光/内驱 P0–P4，**生产代码 0 行**；一手素材深掘亦标注「待实施清单，均未实施」 |
| F6 | **验收靠 pytest 数字** | 测试涨了，用户体感仍差——缺「角色为何这样答」的**可回放因果链**验收 |

---

## 2. 小凌架构：学习未落到实处的证据

| 产物 | 状态 |
|------|------|
| `docs/reports/2026-09-19_经历因果升级机制研究.md` | 研究完成；提出 EventLedger 主轴 + P0–P4 |
| `docs/reports/2026-09-19_情感真源收敛审查.md` | 审查完成；指出情感全族无持久化闭环（P0 前置） |
| `docs/reports/2026-09-19_WrenWen伴侣架构精读.md` | 精读笔记 W1–W28 |
| `docs/reports/2026-09-20_小凌一手素材深掘.md` | 素材深掘 + **待实施清单「均未实施」** |
| 生产 EventLedger / 识海门控 / 心光注入 / 内驱轴 | **不存在** |
| 实际落地 | 记忆隔离、提醒权限、prompt 角色段、user_profile 表 + 智能体工具（浅层） |

**结论（交接给下一窗口必须写进方案首页）**：  
小凌学习 = **文档资产有、工程主轴无**。下一窗口若继续只产报告，判定为**未完成用户任务**。

---

## 3. 下一窗口任务包 AX（必须按阶段交付）

### 阶段 A — 重新调研与克隆（只读学习，产出可审计）

1. **克隆对照仓**（浅克隆即可，路径建议 `D:\Desktop\peer-projects\`，**不入本仓 git**）：
   - nana（二次元 AI 伴侣，backend 全链路）
   - Artemis / sakura（AgentRuntime、五层记忆、curator）
   - MetaPact / nako（Agent Pack：MEMORY.md / HEARTBEAT / scripts）
   - my-raze（memory.ts + shared/memory.ts）
   - SillyTavern（prompt 序列，已有 v1.15 对齐可跳过深读）
   - 可选：generative_agents（记忆流打分）、jiwen（五轴内驱）— 按 GitHub-First 核实活跃度
   - 可选商业侧：星野 / Character.AI / Replika 用户可观察行为（信息类检索，不强制 GitHub）

2. **学习产出强制格式**（每机制一份，缺一不可）：

```markdown
## 机制：<名称>
### 对端实现路径
- 仓库/文件:行号
### 入口 → 中间层 → 落库/落 prompt 的**调用链**
- 逐步写：谁调用谁、传什么参数、异常分支
### 关键数据结构（字段级）
- schema / JSON / 表结构原文摘录
### 核心算法/规则（伪代码或逐步解释）
- 禁止只贴函数名
### 失败与降级
- 超时/解析失败/并发时怎么办
### 可迁移点与不可迁移点
### 对本仓的落点（模块/文件）与验收探针设计
```

3. **禁止事项**：读到「有一个 memory 模块」就停；必须追到 **写库语句 / prompt 拼装函数 / 状态机分支**。

### 阶段 B — 改造方案 + 框架 + 技术选型（设计级，可评审）

交付 `docs/plans/2026-09-xx_智能体架构转型方案.md`（+ 可选 ADR），**必须包含**：

| 章节 | 要求 |
|------|------|
| B1 问题陈述 | 用生产对话/日志实证「串台感/画像乱/玩具感/工具假承诺」的**机制根因**（引用 code 路径） |
| B2 目标架构图 | 一张图：消息 → 门控 → **状态真源** → 记忆/画像/情感/工具 → prompt 槽位 → 回复 → 事件回写 |
| B3 技术选型 | 每层给出：**选什么 / 为何 / 备选与放弃理由**；对齐本仓约束（Python 3.10+、SQLite、微信多用户、MIT、单机 4 worker） |
| B4 与小凌/WrenWen/nana 映射 | 哪些机制采纳、哪些明确不做及理由（硬件/AGI 等 Non-Goal 保留） |
| B5 迁移路径 | P0→P3 **可独立回滚**批次；与现有 `user_facts`/`user_profile`/`affinity_state`/`chat_history` 的兼容策略 |
| B6 验收 | **不止 pytest**：因果回放探针（「角色为何说这句」能从账本/注入槽定位）、双用户串台探针、画像更正探针、工具真执行探针 |
| B7 风险 | 热路径延迟、多 worker、写放大、拟人过度被当 bug |

**技术选型倾向（供评审，非圣旨；下一窗口用源码证据推翻或确认）**：

| 层 | 倾向 | 依据（待源码复核） |
|----|------|-------------------|
| 状态真源 | **EventLedger**（append-only SQLite 表，schema 自 `affinity_records` 扩展） | 小凌/WrenWen「账本宪法」；本仓已有雏形只写不读 |
| 画像/记忆写 | **LLM 工具 + 结构化 extract**（nana JSON / Artemis curator），正则仅兜底 | 已起步 `profile_agent_tools`，需按对端实现补强解析/合并/降级 |
| 会话上下文 | system 槽位化（画像/情绪/关系）+ messages 纯角色轮 + summary 压缩 | nana reply 槽位 + 本仓 v1.31 角色归属 |
| 工具 | 保留 L0/L1 + 限额；结果进 **history 后/PHI 前** 或 tool role | 对标 D 节 |
| 主动消息 | AttentionGate + 阶段间隔 + 发送后衰减（确定性阈值，LLM 只润色） | nana heartbeat / MetaPact |
| 情感 | 先收敛真源再上轴（S1–S6）；禁止第 N 套状态 | 情感真源审查 |
| 框架形态 | **不整仓重写**；在 `shisi/` + `orchestrator/` 增「状态平面」，微信/多用户管线保留 | 对标文档明确结论 |

### 阶段 C — 框架骨架（允许在 worktree 落**可运行空壳**，非功能大爆炸）

- 定义 `EventLedger` 表 schema + 读写 API + 探针脚本（即使消费端尚未全接）
- 定义 prompt 槽位契约文档 + 单测钉死段落顺序（B1 标准序列）
- 定义 profile/memory 工具 schema 与 **对端同构的解析/合并规则**（对照 nana extract 实现）
- 每批附：突变验红设计（故意改坏隔离/槽位 → 测试必须红）

### 阶段 D — 文档与看板

- `docs/research/` 学习笔记（按 §3.A.2 格式，**实现级**）
- `docs/plans/` 或 `docs/adr/` 转型方案
- `docs/board/BOARD.md` 登记窗口与验收
- `DECISION_LEDGER` 记录重大选型裁决

---

## 4. 开窗提示词（复制给下一窗口）

```text
【任务包 AX · 智能体架构转型 · 重大决策】

项目：D:\Desktop\ai-girlfriend（唯一的你）
宪法：先读 AGENTS.md + docs/board/BOARD.md
交接书：docs/HANDOFF_2026-09-21_智能体转型深研与架构.md（本文件，全文精读）

用户裁决（必须遵守）：
1) 重新调研并克隆对标项目到本地（建议 D:\Desktop\peer-projects\，不入 git）
2) 学习规则：必须读懂「如何实现」——调用链、数据结构、算法、失败降级；
   禁止只读函数名/注释/README 就写对标表
3) 产出：实现级学习笔记 + 改造方案 + 框架骨架 + 技术选型（可评审）
4) 这是重大决策转型：禁止「报告堆叠 + 零散补丁」交差
5) 小凌架构：必须说明哪些进生产、哪些不做；EventLedger 等主轴要给出迁移批次
6) 用户已判定上一轮对标改造效果很差——方案里必须有失败模式规避与验收探针
   （因果回放 / 双用户串台 / 画像更正 / 工具真执行）

纪律：
- 一律 worktree（pwsh scripts/new_window_worktree.ps1 -Name agent-x）
- 研究阶段不改生产热路径；实施批次另开、可回滚
- 读代码追到「写库 / 拼 prompt / 状态分支」
- 文档进 docs/ 并 push；对照仓不进服务器

交付清单：
[ ] peer-projects 克隆记录（commit/sha/路径）
[ ] ≥6 份机制实现级笔记（含调用链与文件:行号）
[ ] docs/plans/…智能体架构转型方案.md（含架构图、选型、P0–P3、验收）
[ ] EventLedger schema 草案 + 与 affinity_records 迁移说明
[ ] prompt 槽位契约 + 单测
[ ] BOARD 登记 + DECISION_LEDGER 选型行
[ ] 本窗口内 pytest 基线不回退（角色卡 41 张在位时约 1468+ 通过）
```

---

## 5. 本窗口（主控）已完成、可依赖的现状

| 项 | 状态 |
|----|------|
| 隔离 P0/P1、提醒权限、prompt 角色归属、user_profile 槽、profile 智能体工具 | 已提交并部署（HEAD 见当日本志，health 200） |
| 上一轮研究文档 | 仍在 `docs/research/` / `docs/reports/`，**只作输入，不作完成证明** |
| peer-projects 目录 | **需重新克隆** |
| 小凌生产落地 | **未完成** — 归属 AX |

---

## 6. 验收（下一窗口完成定义）

用户可观察：
1. 角色对「生日/职业/约定」等信息**稳定且可更正**，不再随机编造；
2. 双用户并发无串台体感；
3. 提醒类托付**真落库、真到点**；
4. 方案文档能让评审回答：「状态写在哪、谁读、失败怎么办、如何回放」。

工程：
1. 实现级学习笔记齐全；
2. 转型方案 + 选型 + 迁移批次可评审；
3. 骨架代码/探针在 worktree 可跑；
4. 主检出回归门不回退。

---

**主控窗口职责**：答疑、收编、文档真源；**不**在本窗口做 AX 实施。  
**下一窗口开工第一动作**：读本文件全文 → 开 worktree `agent-x` → 克隆对照仓 → 进入阶段 A。
