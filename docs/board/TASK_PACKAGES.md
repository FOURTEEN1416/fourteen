# TASK_PACKAGES — 窗口级任务包切分（⚠️ **暂缓，未生效**）

> ## ⛔ 状态：暂停使用（2026-09-14 用户批评后撤回）
>
> 用户原话：**「这他妈不要着急着分发任务啊，先将要弄些什么东西确定了啊，急什么？？」**
>
> **批评成立。** 本文件是在**"要做什么"尚未确定**时写的，顺序错了——窗口切分属于执行准备，
> 而当前连**功能范围**都还没定（多模态做到哪一步、嵌入式什么形态、论文投向哪里，均未定）。
>
> **正确顺序**：`确定要做什么` → `立项/拆分` → 功能清单 → 才谈**窗口级切分**。
>
> **保留原因**：其中的**共用红线**与**并行安全矩阵**经论证仍有效，见下方"可复用部分"。
> **生效条件**：待阶段真源（`docs/stages/SPRINT_2026-09.md`）的功能范围被用户确认后，再据此重写。
>
> ---

> 每个窗口**只做一个任务包**，白名单外的文件一律不碰。收编走 `git merge --no-ff wt/<窗口名>` + 主检出回归门。
> 上游真源：`docs/stages/SPRINT_2026-09.md`（阶段真源）｜ 本文件只做**窗口级切分**，不重新定义需求。

---

## 关键约束（所有包共用）

- ❌ **禁止 `git add .`** —— 只提交本包白名单文件
- ❌ **禁止调用 `WebSearch`** 做代码/技术类调研（宪法 §1.3 + §6 零容忍）→ 走 `github-search-strategy`
- ❌ **参赛材料不入库**（宪法 §3 三不入）
- ❌ **不重建 `CODE_GRAPH.md`**，只增量刷新漂移段
- ❌ 不引入兜底层 / 兼容 shim / 重复 owner / 投机抽象
- ✅ 产物落盘即提交；测试基线不可回归

---

## 包划分

### 包 M · 材料线（**无代码改动**）

| 项 | 内容 |
|----|------|
| 目标 | 复赛材料可即交 + 网评材料定型 |
| 产出位置 | `大创赛报名以及后期发展/`（**本地，三不入**） |
| 白名单 | `大创赛报名以及后期发展/**` 仅此目录 |
| 依赖 | B1《赛事指南》口径、B2 系统网址、B3 指导教师实名（**用户自解**） |
| 验证 | 三处逐字一致性 + 规格符合《赛事指南》 |
| 可否并行 | ✅ 与其他包无文件交集 |

### 包 Q · 工程质量线

| 项 | 内容 |
|----|------|
| 目标 | `mypy`/`ruff` 存量债清零；测试补齐；死代码清理 |
| Owner | 按宪法 §2 Owner Map 落到既有模块 |
| 白名单 | 待 `整理开发资料` 后按实际漂移清单确定（**先出清单再开工**） |
| 验证 | `mypy .` / `ruff check` / `pytest` / `cd frontend && npm test`（宪法 §5） |
| 门禁 | **测试基线不回归**；回归即阻塞 |
| 前置 | 复测须沿用 CODE_GRAPH 口径（`create_api_app` 实扫 + `pytest --collect-only` + `vitest list`） |

### 包 V · 多模态通道线（**调研先行，未定型不得编码**）

| 项 | 内容 |
|----|------|
| 目标 | 补上 **语音情绪通道** + **表情通道** |
| Owner | `voice/`（ASR + 语音情绪）、`multimodal/`（表情） |
| 现状 | `voice/` 仅 TTS；`multimodal/` 仅 1 个 processor → **两条通道均为空缺** |
| 前置 | ① 调研（GitHub-First + arXiv 一手源）② 边界 PoC ③ 隐私合规确认（**生物特征采集须用户拍板**） |
| 红线 | 表情/语音属**敏感生物特征** → 本地优先、不落盘原文、显式告知；不得破坏宪法 §3 数据隐私条 |
| 验证 | 单通道端到端真实命令跑通 + 输出接进既有情感画像 |

### 包 E · 嵌入式预研线

| 项 | 内容 |
|----|------|
| 目标 | 承接 `大创赛报名以及后期发展/03-桌面机器人改造设想-草案.md`，出**最小可演示 PoC** |
| 前置 | **用户确认目标形态**（算力/形态决定模型选型：本地轻量 vs 云端） |
| 约束 | 属**推测项**，不得影响当前架构；不承诺量产 |
| 验证 | 实物/日志证据 |

### 包 D · 证据线（软著 / 小论文 / 对标数据）

| 项 | 内容 |
|----|------|
| 目标 | ① 软著提交（不要求当期获批）② 小论文成稿 ③ 对标产品数据用于材料论证 |
| 前置 | Q2 论文目标期刊/会议；Q5 软著申请主体；Q1 对标产品链接 |
| 红线 | 对标数据**只作区间测算 + 方法来源标注**，不得写成我方经营数据 |
| 验证 | 软著提交编号；论文成稿 + 目标清单 |

### 包 G · 治理线（**主检出任，不并行给其他窗口**）

| 项 | 内容 |
|----|------|
| 目标 | 补建 `docs/FEATURE_MAP.md`；维护 `docs/stages/`；处置 2 项未跟踪文件；维持 `docs/README.md` 索引 |
| 约束 | 真源文档**单写**，禁止多窗口并发改 |
| 验证 | `python scripts/check_project_guardrails.py <root> --mode adoption --foundation-gate contract`（若脚本存在） |

---

## 并行安全矩阵

| 包对 | 可并行？ | 理由 |
|------|---------|------|
| M × 其他 | ✅ | 材料线文件完全隔离（且不入库） |
| Q × V | ⚠️ 谨慎 | Q 可能触及 `voice/`/`multimodal/` 同文件 → **先由 Q 出白名单，再定 V 边界** |
| V × E | ❌ | 模型选型互相依赖（算力/形态） |
| G × 任意 | ❌ | 真源文档单写原则 |
| D × 任意 | ✅ | 独立目录 |

---

## 未定项

- 各包白名单**需先跑 `整理开发资料` 出漂移清单**才能锁定（当前仅有阶段级范围）
- 窗口数与分工需用户指定（`SPRINT_2026-09.md` Q6）
