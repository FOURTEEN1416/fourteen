# 功能现状地图（FEATURE MAP）

> 创建：2026-08-24 | 依据：App.tsx 路由实读 + 页面/端点代码核验
> **08-28 治理刷新**：D1 裁决 Demo 全删（F-02 作废）、SP-9 幽灵层删除入册（F 区改处置记录）、端点 204→199 实扫。页面挂载数 15、前端测试基线 59。
> **08-26 前端 src 全文穷举复核**：88/88 源文件 · 11,384 行全部逐行读过（注：该口径为删除前快照）。
>
> **用法**：这是你和我之间的「共同坐标系」。想改哪里，直接报编号（如 `F-07`）+ 说期望，
> 我会按 AGENTS.md 商讨协议先复述、排歧义，你确认后才动手。
>
> **视觉版**：`docs/visual-map/index.html`（本地打开）——全部挂载页面的实拍图册，
> 由 `frontend/visual-tour.mjs` 生成（重拍命令见文件头注释）。截图含真实数据，已 gitignore。
>
> **状态标记**：✅ 完整实现 | 🟡 可用但有已知差距 | ⏸️ 未挂载路由（代码存在）

---

## A. 公开页（无需登录）

| 编号 | 名称 | 入口 | 当前实际行为 |
|------|------|------|-------------|
| F-01 | 登录页 | `/login` | 账号密码登录 + 邀请码注册；登录后跳 `/wechat`（原底部「→ Demo 体验入口」已随 D1 裁决移除） |
| ~~F-02~~ | ~~Demo 体验页~~ | ~~`/demo`~~ | **❌ 已删除（2026-08-28 用户裁决 D1「全删，后续改为产品介绍页」）**：前端 DemoPage.tsx + api/demo.ts + 后端 demo_routes.py(4 端点) + 3 处测试引用全部移除，端点 204→199。产品介绍页为新立项事项，未启动 |

## B. 微信连接域

| 编号 | 名称 | 入口 | 当前实际行为 |
|------|------|------|-------------|
| F-03 | 微信连接控制台 | `/wechat` | 实时状态横幅（连接状态/运行时长/今日消息数/重连次数）；扫码建立连接；凭证有效期内重启自动恢复登录（flock 单例守护） |

## C. 角色域

| 编号 | 名称 | 入口 | 当前实际行为 |
|------|------|------|-------------|
| F-04 | 角色列表 | `/roles` | 卡片式列出全部角色（锚点标签+激活态），直达各角色子页 |
| F-05 | 创建角色 | `/roles/create` | 三方式横向分段（AI 对话提取 / 克隆好友三工具指引 / 文件导入）+ **右侧常驻实时预览卡**（名称/描述/锚点/五维性格条，`lg:grid-cols-[2fr_1fr]` 布局）+ 预设角色快捷选择。⚠️ 06-29 方案A 主体结构已在位，剩余差距见 G-02 修订 |
| F-06 | 角色设置 | `/roles/:id/settings[/:tab]` | 六 tab：基础信息（五维滑条+锚点+口头禅+JSON导出）/ 语音（**仅 MiMo**：三模型选择，克隆/设计标开发中）/ 消息策略 / 数据管理（概览统计+网络人设增强🔥+RAG 静态统计区）/ 表情包 / 剧情时间线（内嵌 StorylineEditor） |
| F-07 | 状态中心 | `/roles/:id/status` | 三卡片：当前情绪 / 亲密等级 / 记忆条目 + 最近记忆列表。🟡 较 v10 设计稿仍缺：情绪分布图、成就体系、亲密度趋势（注：`useEmotionTrend(days)` hook 已就绪未消费） |
| F-08 | 剧情线编辑器 | `/roles/:id/storyline` | 阶段配置（时间窗口/风格规则/行为规则）、进度查询、检测、重置；后端 6 端点齐备 |

## D. 系统设置域

| 编号 | 名称 | 入口 | 当前实际行为 |
|------|------|------|-------------|
| F-09 | LLM 设置 | `/settings/llm` | 多用户隔离：普通用户读写个人配置（回退全局时明确提示来源）；admin 编辑全局默认；供应商清单后端拉取+申请教程弹窗+缓存配置 |
| F-10 | 语音工作台 | `/settings/voice` | 纯 MiMo 工作台（07-27 收敛后仅此一引擎）：引擎卡/可用语音列表/语音克隆上传/音色设计（性别风格年龄音调语速）/合成试听 |
| F-11 | 工具面板 | `/settings/tools` | 内置工具真实健康状态（可用性+错误提示+配置指引）与启用开关 |
| F-12 | 安全设置 | `/settings/security` | 内容安全面板：今日拦截/总检测/拦截率三统计 + 过滤器开关 + 最近安全日志（拦截/放行） |
| F-13 | 日志查看 | `/settings/logs` | 级别过滤/关键词搜索/5s 轮询（可暂停）/TXT 导出 |

## E. 管理后台（admin 角色可见，RoleGuard 守护）

| 编号 | 名称 | 入口 | 当前实际行为 |
|------|------|------|-------------|
| F-14 | 系统用户管理 | `/admin/users` | 注册账号 CRUD（创建/编辑/删除+角色分配+启停）+ 400ms 防抖搜索 + 角色过滤 + 分页排序；不能删自己 |
| F-15 | LLM 供应商管理 | `/admin/providers` | 供应商全生命周期：清单（预设/自定义/特殊三态）/ 启停 / 编辑 / 删除 / 申请教程弹窗 / 添加自定义 OpenAI 兼容源 |

## F. 幽灵层 → ❌ 已处置（SP-9 删除 + D1 Demo 全删，2026-08-27/28）

> **2026-08-28 状态更新**：原「幽灵层」三页（UsersPage/UserWorkspace/BindingDetailPage）已按 SP-9 裁决于 08-27 删除（连带 api/users.ts、wechat.ts 绑定区块、useWechatBindings 及三页测试）；本图 08-26 版记录的复活价值随删除作废，如需恢复从 git 历史找回。后端 /api/wechat/bind* 端点保留。原 G-19 KnowledgePreview / G-20 幽灵数据层 / G-21 心理画像 / G-22 收藏转发 状态不变（仍在库零消费/零挂载）。

## G. 后端核心能力（API 层，199 端点 / 20 个路由文件，2026-08-28 实扫）

| 编号 | 能力 | 关键行为 |
|------|------|---------|
| B-01 | 微信直连 | wechat_direct/ 扫码登录、收发消息、多用户独立会话、凭证自动续期 |
| B-02 | 对话编排 | orchestrator/ 热路径 process_message（9 阶段初始化 + 会话锁）；SSE 真流式优先、伪流式降级 |
| B-03 | 情感引擎 | 8 级好感度阶梯（陌生人→羁绊）、情感状态实时更新 |
| B-04 | 三层记忆 | 工作记忆（20 条滚动）/ 情景记忆（重要性评分+自动遗忘）/ 语义记忆（事实提取），按 user_id 强隔离 |
| B-05 | 主动搭话 | ASE 引擎：久未聊天/早安晚安/情绪低落触发；每日上限 + 最小间隔 + 冷却期 |
| B-06 | 语音合成 | MiMo 云（含 voiceclone/voicedesign 模型切换）/ Edge-TTS / 本地模型 fallback 链；全部端点已接 X-API-Key 认证 |
| B-07 | 风格克隆 | 本地聊天记录提取 → 上传 → 服务器分析（LoRA 本地训练已于 07-30 移除） |
| B-08 | 知识 RAG | shisi/knowledge/ RAGEngineV2 检索 + 角色知识富化 |
| B-09 | 安全四件套 | 内容过滤 / PII 脱敏 / 加密管理 / 提示词注入检测 |
| B-10 | 工具系统 | 天气/搜索/日历/提醒/计算器内置工具 |
| B-11 | 认证双体系 | JWT（用户登录，refreshToken 内存闭包 + httpOnly cookie）+ X-API-Key（服务间，hmac 恒时比较） |
| B-12 | 基础设施 | PostgreSQL 生产库 / Redis 可选缓存 / systemd + nginx 部署 / Prometheus metrics |

## H. 已知残留差距（2026-08-26 前端通读后修订）

| 编号 | 描述 | 来源 |
|------|------|------|
| G-01 | 状态中心丰富化（情绪分布/成就/趋势）——**数据层 hook 已就绪（G-20），缺的只是 UI 消费** | v10 设计稿遗留，🟡 工作量下修 |
| ~~G-02~~ | ~~创建角色缺实时预览面板~~ → **❌ 已过时：预览卡已在位（见 F-05）**。剩余真实差距仅为规格细节核验（快速设置子 tab 等是否需要） | 06-29 方案A 对照 |
| G-03 | HTTPS 未启用（裸 IP 无法签证书，域名+证书需费用——**用户决策暂缓**） | OBS-2 |
| G-04 | 服务以 root 运行（改 www-data 需权限迁移+停机窗口） | OBS-3 |
| ~~G-05~~ | ~~三层级动态侧边栏~~ | **❌ 用户裁决不需要**（2026-08-24） |
| G-06 | RoleSettings 语音/消息/表情包三 tab 的保存接口标「开发中」（前端仅预览配置） | 页面内实况标注 |
| G-07 | DataTab 的 RAG 区块为静态占位统计（character.rag 数据源未接真实接口）；活的知识功能仅网络增强按钮；KnowledgePreview 零挂载 | 前端通读发现 |
