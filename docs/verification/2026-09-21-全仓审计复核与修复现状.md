# 全仓审计复核与修复现状（2026-09-21 下午）

> 任务：用户指令「全仓通读，进行审计，忽略 API key 泄露类基础安全问题，关注真正的功能和性能问题」。
> 方法：主检出（`D:\Desktop\ai-girlfriend`）只读通读热路径全链（orchestrator → persona → memory → llm → tools → proactive → wechat → api 路由层），**并以 `0bdb91e` + 工作树在制品为基线，对同日早批审计报告 `2026-09-21-full-code-audit.md`（10 P0 + 53 P1/P2）逐条对码复核修复状态**，另独立补查新缺陷。
> 基线声明：角色卡 **41 张在位**；工作树含他窗在制品 5 文件（`api/run_api.py` / `api/websocket_server.py` / `proactive/reminder_delivery.py` / `wechat_direct/connector_registry.py` / `tests/test_reminder_intent_pipeline.py`）+ 2 个未跟踪临时脚本。本报告零代码变更。
> 探针：`pytest tests/test_reminder_intent_pipeline.py`（含 WIP 改动）**36/36 通过**（Python 3.12.4）。

---

## 一、旧报告 P0 修复状态对账（10 项）

| # | 缺陷 | 状态 | 证据 |
|---|------|------|------|
| P0-1 | 记忆 dict 被 str() 打碎、长期记忆整体蒸发 | ✅ 已修（`0bdb91e`） | `context_budget.apply_budget` dict 保型直通（`context_budget.py:188-215`）；`persona_service.py:105` isinstance(dict) 分支恢复生效 |
| P0-2 | 画像库路径差三级、构造即抛被吞 | ✅ 已修（`0bdb91e`） | `user_profile.py:267-272` `default_store` 锚定 `PROJECT_ROOT / "data" / "sqlite.db"` |
| P0-3 | 知识上传 index() 替换语义清库 | ✅ 已修（`0bdb91e`） | `knowledge_routes.py:202-215` 改 `add_chunks` 追加 + 计数以 `len(retriever._chunks)` 为准 |
| P0-4 | 隔离四例（facts 全库读 / rag dict-repr / ASE 裸键广播 / working 混档） | ✅ 已修（`0bdb91e`） | `misc_routes._memory_scope_prefix` 前缀过滤（:35-52, :247-275）；`safety_routes.py:126-138` 传 str + to_thread；`scheduler._collect_ase_user_keys` 只产完整键（:646-694）；`_legacy_working_memory.py` 按 session 分桶；`run_api.py:231-234` 坏键拒发 |
| P0-5 | websocket 无认证广播 + 登录风暴 | 🟡 主体已修 | WS 认证（JWT/API Key/首帧）+ 并行广播 + JWT 归属前缀（`websocket_server.py:34-39, 86-160`）；登录风暴 SSE 已被 09-19 通道重构移除。残留归入 P0-7 |
| P0-6 | web 提醒假送达（协程未 await 即 return True） | ✅ 已闭合 | `741b5d2`（`run_api._ws_send` async+await、`reminder_delivery._send_to_session` awaitable 处理、测试异步契约回归钉「失败不得记 delivered」）——**回归 `pytest tests/test_reminder_intent_pipeline.py` 36/36 绿** |
| P0-7 | 四 worker 架构性互盲 | ✅ **部署单例规避闭合（`741b5d2`），架构互盲免疫尚未建** | `deploy/ai-girlfriend.service` `ExecStart` 写死 `--workers 1`（提交注释指名 P0-7：通道 registry / scheduler / LLM 网关 / 知识索引均为进程内单例）；`character_knowledge_service.py` 补 `self._index_mtimes` 磁盘 mtime 核对。**但这不是搭跨进程共享/选举/队列的免疫架构，而是把问题范围框死在单进程里。** 若后续恢复多 worker 或引入其他有状态单例，互盲问题会复活，需另做进程间状态真源/总线设计。 |
| P0-8 | 通道 flock fd 不释放 → 同进程重连恒 429 | ✅ 已修（**工作树 WIP，未提交**） | `connector_registry._release_poll_lock` + disconnect/remove 释放（flock LOCK_UN + os.close） |
| P0-9 | v1.31「记忆 ≠ 对话历史」改线被回退 | ✅ 已修 | `persona_service.py:95-182`：记忆独立段 + `ROLE_CLARITY_RULE` + `prompt_builder.build(chat_history="")` |
| P0-10 | agent-plane 6 端点无 admin/归属校验、curate 全库破坏 | ✅ **已闭合（`741b5d2`）** | `api/routers/agent_plane_routes.py` 的 replay/profile/events/curate/probes 五个端点依赖均已改为 `_admin: tuple[int, User] = Depends(require_role("admin")),`（提交注释说明由 `verify_api_key_dep` 升级为 admin-only），接口层控制面越权面闭合。**但这不代表所有归属隔离都完善**，仍需关注个别接口可能存在的归属校验缺口。 |

## 二、P1 抽查复核（按域采样 20+ 条，全部行级对码）

**仍开（证实）**：
- **#8** `conversation_summarizer.py:45` 缓存键 `session:older_count` 每轮漂移 → 51~80 条区间每轮一次同步 LLM 摘要；`_cache` 无淘汰无界增长。
- **#9** `reflection_engine.py:196-198` `filter_dict={"type":"reflection"}` → `vector_memory.py:348-355` collection_map 仅 episode/fact → 落「扫全部 collection」分支，每轮 O(6×k)。
- **#11** `tools/base_tool.py`：`dispatch` 从不消费 `self.timeout`（`execution_timeout_seconds=10` 死配置）；`_check_rate_limit` 按工具名**全局** 3/分钟 → 第 4 个用户起被拒且记成工具故障；重试同步 `time.sleep`。
- **#16** `multi_provider_gateway.chat_stream`（:459-486）只用 `current_provider` 无 fallback——首要 provider 宕机时 web 流式裸死；`chat()` 直接写 `_current_index` 污染共享指针（并发下流式/工具调用落到错误供应商）。
- **#21** `main.py:397-410` 微信形态 `_send(msg)` 无 session_key 形参；`scheduler._send_targeted:960-961` async 分支**无** TypeError 回退（回退只挂同步分支 :963-967），TypeError 被外层 except 吞 → 通道置 None → 落广播兜底。另：同步旧签名回退分支仍记「定向投递成功」（:968）——**广播记成定向成功**，谎报残留。
- **#22** `wechat_connector._cleanup_context_tokens`（:1196）零调用者（全仓 grep 仅定义处命中）；_received_msgs 裁剪同理。读侧 TTL 过滤兜底，影响轻微，但「定期清理线程」是纸面机制。
- **#24** `scheduler._check_important_dates`（:1095/1102）把裸 `_dt.now()` 喂给已收口 `now_local` 的 `check_today`，dedup_key 也不同钟——UTC 主机上北京 00:00-07:59 命中算前一天。
- **#26/#27/#28/#29/#30** wechat 域五条全证实：`_msg_executor.submit` future 丢弃 + `_handle_message` 前半段裸奔无锁；每消息 `asyncio.run` 新循环 × `session_locks` 跨循环缓存 `asyncio.Lock`（同用户连发第二条进 waiter 路径即跨循环唤醒失灵）；`peer_character.try_handle_character_choice/build_character_menu` 生产零调用（「回复『角色』自选」整链未接线）；`save_session_state` 无锁 RMW + 非原子写（同文件 context_tokens 已做 tmp+os.replace，双标）；`send_voice/send_image/send_emoji` 仍留 `_last_user_id` 回退（隔离护栏缺一腿）。
- **#32** `misc_routes.py:104-105` `await get_wechat_status()` 绕过 FastAPI DI，`credentials` 拿到 Security 标记默认值 → `_try_user_id` 抛异常被吞 → dashboard 微信状态**恒「未连接」**且写入 5s TTL 缓存反哺。
- **#35** `tools_routes.py:73-79` toggle OFF = `unregister` pop 实例；再 ON 时 `registry.get` → None → 404，工具从管理界面消失直到重启（单向死路）。
- **#40** `emotion_routes PUT /params` 改的是模板引擎 `_config`；聊天路径用 `_get_request_emotion_engine` 的**请求级引擎**（`optimized_orchestrator.py:679-683` 构造时**不传 config**）→ ① 模板引擎聊天路径根本不用；② 请求级引擎全部跑默认参数。**双重断链，该端点对生产零效果**（比原报告更坏，机制已钉死）。
- **#45** `mimo_tts_provider.py:156/428` 每次合成/克隆新建 `aiohttp.ClientSession`（每次 TCP+TLS 握手；voiceclone 失败要等 `_timeout` 秒才降级基础模型，再等一次才落本地）。
- **#46** `EmotionEngine` 全类无 `threading.Lock`（grep 证实）：`analyze` 读写 `_state/_total_chats/_emotion_history` 无同步；会话锁只保证 orchestrator 串行，挡不住 `scheduler._run_daily_maintenance` 的 `apply_time_decay`（:1016-1022）与聊天路径并发写同一引擎。
- **#48** `scheduler._llm_proactive_one_user:798` `getattr(eng, "_character_id", "")`——ASEEngine 真实属性是 `_knowledge_character_id`（`ase_engine.py:701`），恒空 → `load_persona_hint("")` 返回空 → **主动消息「人设提示」恒空**。
- **#49** wait 门控已落地（`_llm_proactive_next_ok`，1ec283d）✅；但**静默时段 LLM 前置闸仍缺**——`_llm_proactive_one_user` 在 `decide_proactive`（远端 LLM 调用）之前无 `_is_quiet_hours()` 短路，23:00-07:00 每 5 分钟 × 每用户照烧 token（消息不发出，钱照付）。
- **#51** `data/agent_plane.db` 只 append 不 prune（`web_disabled` 在关闭态每 tick 每用户一行，关闭反而涨得最快）。
- **#52** ① `load_persona_hint` CWD 路径 ✅ 已修（`llm_proactive.py:195-200` 走 `project_path`）；② `agent_plane_routes.py:106` `Path("data/sqlite.db")` CWD 相对 ❌ 仍开。
- **#53** `scheduler.write_config_file`（:437-439）直接 `write_text` 全文件 RMW，无锁无 tmp+rename——4 worker 下并发写半文件风险。

**已修（复核证实）**：
- **#50** TOOL_RESULT 事件已带 `turn_id`（`optimized_orchestrator.py:1005-1010`）✅。
- **#15** LLMGatewayV2/OpenAICompatibleProvider 换 loop 时**有** `call_soon_threadsafe(aclose)` 调度（`llm_gateway.py:108-114`、`openai_compatible_provider.py:115-124`）——**原报告「直接换绑不 aclose」措辞过重**：实为「尝试关闭但把旧 client 的 aclose 调度到新 loop + 异常 suppress」，httpx/anyio 下多数能关掉，属部分缓解。建议改为「旧 loop 存活则投回旧 loop，否则同步强关」。

**未复验（沿用原报告结论）**：#1-7、#10、#12-14、#17-20、#23、#25、#31、#33-34、#36-39、#41-44、#47；P2 全域。

## 三、本轮新发现（旧报告未载）

| # | 级别 | 缺陷 | 位置与机理 |
|---|------|------|-----------|
| N1 | P1 | **定向投递失败兜底成「广播 + 谎报成功」** | `scheduler._deliver:936-946`：`_send_targeted` 抛异常 → `self._send(message)`（main.py 形态的全员遍历/广播发送器）→ `return True` 记「已送达」。与 P0-4「坏键拒广播」精神相悖：一条发不出去的私信变成全员广播且账面成功 |
| N2 | P1 | **重要日期祝福多用户语义未定义 + 全局角色口吻** | `scheduler._check_important_dates:1127` `self._deliver(message)` 无 session_key → `_send_to_all` → 给**所有用户的所有绑定 peer** 发同一祝福；`active_id` 取全局激活角色——A 的用户收到以 B 选的角色口吻写的「生日快乐」。多用户时代「角色生日是全站事件还是 per-user 事件」从未定义（叠加 #24 时钟缺陷） |
| N3 | P2 | **请求级情绪引擎不继承配置 → emotion 参数端点彻底无效** | 见 P1-#40 机制补全：`_get_request_emotion_engine`（`optimized_orchestrator.py:679-683`）构造不传 `config`，模板引擎又非聊天路径所用——`PUT /api/emotion/params` 是**纯死端**（改了谁都不生效），且请求级引擎永远跑默认值 |
| N4 | P2 | **`WorkingMemory.start_session` 重置既有桶** | `_legacy_working_memory.py:52` `self._buckets[sid] = deque(...)` 直接覆盖——`/api/session` 对同 id 重复调用会清掉该会话内存工作记忆（DB 是真源，影响小，登记） |
| N5 | P2 | **提醒判死过快：按次数而非时间窗** | `reminder_delivery` 每分钟轮询 + `mark_reminder_result` 3 次判死（`structured_memory.py:1048-1054`）→ 微信通道抖动 3 分钟，提醒即永久 failed。「次数」与「轮询频率」耦合：轮询 1 分钟 = 3 分钟判死。建议按时间窗（如到期后 30 分钟内持续重试） |
| N6 | P3 | **BYOK 检查吞非 HTTP 异常** | `chat_routes.py:82-83/146-147` `except Exception: pass`——`ensure_user_has_key` 内部 bug 会静默放行（仅登记，概率低） |
| N7 | P3 | **临时脚本遗留（L8 违例）** | `scripts/_tmp_dump_memory_state.py`、`scripts/_tmp_wipe_memory_data.py` 未跟踪（后者是对生产库整表 DELETE 的清库脚本，误执行即数据销毁）——建议删除或移出仓库目录 |

## 四、优先级建议（修复顺序 = 每轮必现 > 数据销毁 > 成本流失 > 体验）

1. **立即**：P0-8 在制品**提交 + 部署**（A 档闭环未走完）；P1-#49 静默前置闸（一行 `_is_quiet_hours()` 短路，止损夜间 token 空烧）；P1-#48（`_character_id` → `_knowledge_character_id`，一行修复人设恒空）。
2. **本批应做**：P1-#11（工具超时接线 + 限速改 per-user）；P1-#16（stream fallback + 不污染 `_current_index`）；P1-#27（跨循环锁：会话锁按 loop 分键或微信侧复用常驻循环）；P1-#32（dashboard 状态走 `get_wechat_state(user_id=...)` 而非绕过 DI）；P1-#35（toggle 改 enabled 标志位而非 unregister）；N1（失败不兜底广播）；N3（请求级引擎继承 config 或端点改作用域）。
3. **排期**：P0-7（架构：调度器/WS/记忆缓存外置或单 worker + 进程内总线）；P1-#8/#9（记忆层缓存键与集合路由）；P1-#26/#28/#29/#30（wechat 并发与隔离护栏补腿）；P1-#21（main.py 形态要么补 session_key 形参要么标注废弃）；P1-#45（TTS session 复用）；P1-#46（EmotionEngine 加锁）；N2（重要日期 per-user 化）；N5（判死改时间窗）。
4. **卫生**：N7 临时脚本清理；P2 域（apps.log 双写、users 表 ON DELETE、worker 数钉死、唯一 uvicorn 入口）随对应批次顺带。

## 五、完成声明四要素

- **验证证据**：所有「仍开」条目均附当前 HEAD（`0bdb91e`）+ 工作树行级证据；「已修」条目附修复提交号与现行代码行；WIP 测试 `test_reminder_intent_pipeline.py` **36/36 通过**实跑。
- **边界检查**：只读审查，零代码变更；未跑全量分块基线（零改动，沿用 1455/1451/4 + 41 卡口径）；服务器运行态取证未做（P0-7 的 ws 端口冲突为机制推断，建议部署侧 `ss -tlnp | grep 8765` 实证）。
- **已知限制**：P1 未复验条约 25 条沿用早批报告（其本身已经主审行级核验）；前端域本轮未深读；成本量化（288 次/天等）为配置推算非生产实测。
- **置信度**：P0 状态映射全高（commit/行号双证）；P1 抽查项高；新发现 N1-N7 高（行级）；未复验条目中-高。
