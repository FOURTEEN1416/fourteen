# W4 验证窗口 · 报告（2026-09-14 · 包 T 阶段一）

> 关联事故记录：同目录 `INCIDENT-2026-09-14-主仓工作树与git元数据损毁.md`
> 执行分支：`wt/verify` ｜ 工作区：`D:\Desktop\ai-girlfriend-verify`（幸存副本，检出点 `e9a063b`）

---

## 结论（先行）

| 项 | 结果 |
|----|------|
| **全量 pytest 通过率** | ✅ **986 / 992 = 99.40%**（986 passed · 6 skipped · **0 failed**，用时 154.35s） |
| **失败清单（基线）** | **空**（0 failed；6 skipped 为「空参数化」跳过，非失败） |
| **失败清单（新增 TDD 用例）** | **3 个，全部预期红**（`TestHandleMessage` 图片/语音收包，见 §2） |
| **复核结论** | N/A —— W3 未建窗（`D:\Desktop\ai-girlfriend-code` 不存在）、主仓 git 已毁，无 diff 可复核 |
| **置信度** | 通过率与失败清单：**高**（本机实测、命令可复现）；口径差归因：**中**（见 §3） |

> ⚠️ **执行环境偏离说明（诚实标注）**：任务指定的解释器 `D:\Desktop\ai-girlfriend\.venv\Scripts\python.exe` **已随主仓被删**，全盘无本项目 venv。本报告改用 **系统 Python 3.12.4**（已装 pydantic / fastapi / sqlalchemy / chromadb / pandas / numpy / pytest-asyncio / pytest-mock 等，仅缺 `sentence_transformers`、`structlog`）执行，并按要求 `PYTHONPATH=` 清空 safe-delete 护栏。

---

## 1. 任务① · 全量 pytest 基线（已完成）

**命令（实测）**：
```bash
cd D:/Desktop/ai-girlfriend-verify
PYTHONPATH= "C:/Program Files/Python312/python.exe" -m pytest -q --no-header -p no:cacheprovider
```

**结果**：
```
collected 992 items
986 passed, 6 skipped in 154.35s (0:02:34)
```

- **0 失败**、0 收集错误 → 现有测试套件在该检出点**全绿**。
- 6 skipped 全部来自**空参数化**（如 `tests/test_persona_injection.py` 的 `config/characters/*.json` 为空 → 参数化为 0 项，pytest 记为 skipped）。

**回归确认（写入新增用例后复跑全量）**：
```
3 failed, 986 passed, 6 skipped in 138.66s (0:02:18)
```
→ **既有 986 项通过数零变化**（无回归）；仅新增 3 项按设计报红。基线不回归 ✅

---

## 2. 任务② · `type:3`(图片) / `type:34`(语音) **收包**测试（已完成，预期红）

### 2.1 缺口根因（本窗口实测定位，非推测）

`wechat_direct/wechat_connector.py:747-749`：
```python
msg_type = raw_msg.get("message_type", 0)
if msg_type != 1:      # 只看用户消息
    return
```
该入口守卫把 `message_type ∈ {3, 34}` 的**图片/语音消息在入口直接丢弃**，使同文件 770-780 行 item 级 `type ∈ {1, 34, 3}` 的解析对媒体消息成为**死代码** —— 即任务包 V2 缺口（「`image_data` 全仓无消费者」）的**真实入口**。

**佐证**：`tests/test_wechat_connector.py` 原有 2 个 3/34 用例（第 56、68 行）**只覆盖「发送」**（`_send_voice_message` / `_send_image_message`），**收包侧零覆盖**。

### 2.2 新增用例（写入 `tests/test_wechat_connector.py::TestHandleMessage`）

| 用例 | 断言 |
|------|------|
| `test_image_message_type3_is_routed` | `message_type=3` + `item_list[{type:3,image_item:{image_data}}]` → 必须路由到 `_call_user_manager` 且发一次回复 |
| `test_voice_message_type34_is_transcribed_then_routed` | `message_type=34` + `item_list[{type:34,voice_item:{voice_data}}]` → 必须先触发 `_transcribe_voice`（ASR），再以**转写文本**路由 |
| `test_voice_message_type34_falls_back_when_asr_unavailable` | ASR 返回空串（`enabled:false` 现状）时仍须进入链路，**不得静默丢弃** |

### 2.3 实测结果（预期红 ✅）

```
collected 13 items
tests\test_wechat_connector.py ..........FFF      [100%]
3 failed, 10 passed in 0.50s

FAILED TestHandleMessage::test_image_message_type3_is_routed
FAILED TestHandleMessage::test_voice_message_type34_is_transcribed_then_routed
FAILED TestHandleMessage::test_voice_message_type34_falls_back_when_asr_unavailable
```
失败点全部指向**入口守卫**（`_call_user_manager.call_count == 0`、`_transcribe_voice.call_count == 0`）—— 与 §2.1 根因**完全吻合**，非测试写法问题。

> **给 W3（包 V2）的验收口径**：修掉 `msg_type != 1` 守卫（放行 3/34，或改为按 item_list 判定）后，上述 3 用例应转绿，且**不得使既有 986 项回退**。

---

## 3. 口径差分析（992 vs 文档 1048/1042）

| 来源 | 收集数 | 说明 |
|------|--------|------|
| `AGENTS.md` §4.3 / CODE_GRAPH v3.5.0 | **1042** Python | 文档口径 |
| 主控窗口 2026-09-14 实测 `--collect-only -q` | **1048** | 本仓 `.venv` 环境 |
| **本窗口实测（`e9a063b` + 系统 Python 3.12）** | **992** | 差 56 |

**已定位主因（证据）**：`tests/test_persona_injection.py:21` 以 `Path("config/characters").glob("*.json")` 做参数化，而**工作树内 `config/characters/` 为空目录**（该目录下的角色 JSON 属**未跟踪夹具**，未随 git 检出）→ 参数化坍缩为 0 项 → 整族用例转为 skipped。**主仓被毁后，这批夹具与 `.venv` 一同丢失，故 992 无法与 1048 完全对账。**

**次要因素**：系统 Python 缺 `sentence_transformers` / `structlog`，可能影响个别可选依赖门控的收集（未观测到收集错误，影响有限）。

> **结论**：992 是**当前幸存副本 + 可用解释器**下的真实可复现数字；与 1048 的差额**部分属夹具缺失（事故后果）**，**不能据此判定测试回归**。要拿到权威基线，须在**恢复后的仓库 + 原 `.venv`** 上复跑。

---

## 4. 任务③ · 验证报告（本文件）

已落盘 `docs/verification/`。

## 5. 阶段二 · 独立复核 W3（⛔ 阻塞）

前置全部缺失：`D:\Desktop\ai-girlfriend-code` 不存在、`wt/code` 未建、主仓 git 已毁。

## 6. 提交状态（⛔ 无法提交）

主仓 `.git` 元数据已毁 → **`git add` / `git commit` 均不可用**。本窗口的产物（本报告 + 事故记录 + 测试改动）**已在幸存副本内落盘**，并已同步至保全备份 `D:\Desktop\ai-girlfriend-verify-backup-2327`。恢复 git 后按白名单精确提交：

```bash
git add tests/test_wechat_connector.py
git add docs/verification/
```

## 7. W4 未做（红线遵守）

- ✅ 未修改任何**实现代码**（仅改 `tests/test_wechat_connector.py`）。
- ✅ 未对主仓执行任何删除/移动/恢复/git 写操作。
- ✅ 未 `git add .`（事实上 git 不可用）。
- ⚠️ 附注：全量测试会在工作树内生成 `data/`（`users.db`/`sqlite.db`/`chroma_db`/`knowledge` 等）；该目录在 `.gitignore` 内，不影响提交。

---

## 8. 附：幸存副本保真性检查（唯一副本完整性验证）

主仓已毁，`D:\Desktop\ai-girlfriend-verify` 是**全机器唯一完整副本**，故对其做**全量**（非采样）保真检查：

| 检查项 | 方法 | 结果 |
|--------|------|------|
| Python 语法 | 354 个 `.py` 全量 `ast.parse`（`utf-8-sig`） | ✅ **0 错误** |
| 空/截断文件 | 全树非空校验 | ✅ 仅 `tests/core/__init__.py` 为空（**包标记文件，正常**） |
| YAML 解析 | 11 个 `.yaml/.yml` 全量 `yaml.safe_load` | ✅ **0 错误** |
| JSON 解析 | 7 个 `.json` 全量 `json.loads` | ⚠️ 1 处：`pyrightconfig.json` |
| 文件清单 | 扩展名统计 | `.py` 354 · `.md` 74 · `.tsx` 72 · `.ts` 38 · `.yaml` 10 · `.json` 7 · `.sh` 6 … |

**唯一发现（低危，非损坏）**：`pyrightconfig.json:15` 在 `"clone_training",` 后带**尾逗号**（第 16 行 `]` 前）→ 严格 JSON 非法，但 pyright 使用 JSONC 容忍它。**属仓库既有瑕疵**（随 `e9a063b` 检出），非本次事故造成的截断/损坏，**不阻塞恢复**。建议（非本窗口职责）后续修掉。

> 结论：**幸存副本结构完整、无截断、无损坏**，可作为恢复源直接使用。

---

*W4 验证窗口（歆歆）· 2026-09-14 · 写于幸存副本内*
