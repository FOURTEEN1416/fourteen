# 2026-09-17 三连修复验证报告（人设/emoji/主动消息 + web 开关 + 死代码清洗）

> 验证人：zcode 主控窗口 ｜ 提交链：`10c8f0f` → `5e4ecb5` → `8a34b23` → `4f6ed29`（全部已 push，服务器已部署）
> 方法：生产日志取证（修复前后对照）+ 全量回归 + 远端端点实测

---

## 一、修复前生产实证（问题存在的证据）

来源：`/opt/ai-girlfriend/data/app.log`（2026-09-15~17，5.6MB）

| 问题 | 证据 |
|------|------|
| emoji 泛滥 | 8/8 真实回复全部带 1-2 个文字 emoji（`'嗨！你好呀，今天心情怎么样呢？😊🌟'` 等） |
| 人设漂移 | 17:34 回复"我是十四…人工智能助手"→ 17:39 同会话"我是林挽夏"（绑定 17:36 创建+服务 17:38 重启前后内存态断裂） |
| 主动消息 0 送达 | 64 次 `ASE triggered` 全部落入 `[orchestrator.optimized] [主动消息]` console 兜底；"微信主动发送"日志 count=0（微信当时在线 connected:true） |
| LLM 生成失效 | 触发的全部是模板消息（"告诉你个事"）；根因 MultiProviderGateway 无 chat_sync |

## 二、修复后验证（先证明再宣称）

### 1. 主动消息端到端送达（核心实证）
```
2026-09-17 19:14:54 [scheduler] INFO: ASE triggered: [share] 哼，这么晚了还打扰我，不过...你还好吗？别太晚睡哦。🌙
2026-09-17 19:14:54 [wechat_direct] INFO: 微信主动发送成功: 哼，这么晚了还打扰我，不过...你还好吗？别太晚睡哦。🌙
2026-09-17 19:14:54 [scheduler] INFO: 主动消息已投递: wechat
```
三重证明：送达微信（旧 0/64）+ 内容非模板（chat_sync LLM 生成生效）+ 目标为绑定 wxid（get_bound_wxids 定向）。

### 2. 测试回归
- 基线（修复前）：1015 通过 / 4 跳过
- 批次一后：1028 通过 / 4 跳过（+13 新用例，零回归）
- 批次二后（删指令测试 -18/-2 + 开关用例 +2 + 端点计数更新）：**1014 收集 / 1010 通过 / 4 跳过**
- 前端：vitest **87/87**、tsc --noEmit **0 错误**（含死代码删除后）

### 3. 远端部署验证（2026-09-17 20:45，remote_deploy 全流程含服务端前端构建）
- `systemctl is-active` = active；`/api/health` = {"status":"ok"}
- `GET /api/knowledge/collect-config` → `{"enabled":false,"interval_minutes":60,"available":true}`（跨 worker 一致性生效；修复前非 master worker 返回 available:false）
- `GET /api/proactive/config` → 含 `quiet_hours_start:23 / quiet_hours_end:7`（新增字段就位）
- `frontend/dist/index.html` mtime = 20:45（前端已重建，UI 开关上线）
- 启动日志："已加载 1 条微信绑定" + "微信登录成功"

### 4. 服务器数据面
- `config/characters/` 25 张卡（24 张同步 + 绑定卡 62105bca），Python JSON 校验 0 bad
- `data/users.db wechat_bindings` 1 条（wxid→62105bca）保持不变

## 三、留待用户实测（无法替代的最终验收）
1. 微信发"你是谁"→ 应回答绑定角色名（非"十四/AI 助手"），emoji 每条 ≤1 或无
2. 控制台"设为活跃"切卡 → 微信下一句人设即时切换
3. MessageTab 调免打扰时段保存 / DATA tab 开"定期采集"（默认关）

## 四、未验证项（诚实声明）
- 免打扰时段调整后的实际拦截行为（需在设定时段等 ASE 触发观察"免打扰时段…跳过"日志）——逻辑有单测覆盖（`test_scheduler_quiet_hours_settable`），生产行为待自然观察
- vault 定期采集开启后的实际采集量（服务器当前开关为关；开启后下个 interval 出"知识库定期采集完成: N 个角色"日志即可证）
- 微信实测两项（上节）依赖用户手机操作

## 五、处置登记（未修但有结论）
| 事项 | 处置 |
|------|------|
| `proactive_messenger.py` / `sticker_adapter.py` 悬空 | DELETION_LOG 登记，留观（表情包立项可能复用） |
| 晚安触发窗 22-24 与免打扰 23 点的重叠张力 | 免打扰现已 web 可调（用户可自行把结束调到 8 点等缓解），架构性重设计未立项 |
| 免打扰/阈值等 ASE 参数仍为运行时态（重启回默认） | 免打扰/采集已持久化（scheduler_config.json）；ASE 阈值等保持原语义未动 |
