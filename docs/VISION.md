# VISION — 「唯一的你」终极状态宣言

> **创建**：2026-08-26 | **维护者**：随产品级决策更新 | **性质**：现行真值（L1）
> **来源**：76 份载体穷举读取后合成（54 文档 + 12 配置 + 20 角色卡），经用户批复校准
> **分工**：`DECISION_LEDGER.md` 管决策生死 → 本文件管终态合成 → `FEATURE_MAP.md` 管功能实况 → `CODE_GRAPH.md` 管代码实况
>
> ⚠️ **合成纪律**：本宣言只写「已被验证的既成事实」与「宪法级约束」。
> 一切定稿但未获批准的方案一律放 🅿️ 候选池，**绝不冒充终态**（2026-08-26 用户批复）。

---

## 一句话定位

**微信扫码即用的多用户 LLM 情感陪伴系统**——每个用户绑定专属角色卡，在真实微信里获得有记忆、有情绪、会主动搭话的长期陪伴关系。品牌名「唯一的你 / unique-you」，主角色「十四」。

---

## 一、产品本质（不变的内核）

源自 persona.yaml / emotion.yaml / emotion_style_matrix.yaml 的设计基因，自 05-19 立项至今未变：

| 内核 | 载体参数 |
|------|----------|
| 情感不是标签，是状态机 | 8 级好感阶梯（陌生人→羁绊）、能量衰减/恢复、强度自然衰减 |
| 性格是可计算的 | 五维人格（warmth/playfulness/independence/jealousy/stubbornness）+ 情感→风格映射矩阵（9 情感 × warmth_delta/emoji/句长/修辞） |
| 记忆分三层 | 工作记忆 20 条 → 情景归档（20 轮触发）→ 长期 ChromaDB 向量库 |
| 陪伴是双向的 | ASE 主动消息引擎（日上限 8 条、间隔≥30min、LLM 生成、按时段分早午晚） |
| 关系要慢慢养 | 亲密度初始 0、解锁阈值 25/50/75/90（话题→表情包→语音→特殊互动） |

---

## 二、终态版图三分法

### ✅ A. 既成事实基线（现状 = 已验证，测试 1104 全过）

**产品形态**
- 微信扫码登录，文字+语音聊天，多用户严格隔离（user_id 全链路隔离）
- 19 页管理控制台（邀请码注册 + 角色管理 + LLM 配置 + 数据看板等）
- 角色系统：SillyTavern V2/V3 PNG 卡兼容 + 自有角色卡体系（20 张现役卡，💊熟悉度/心跳值/手写板等玩法均支持）
- 204 个 API 端点 / 17 路由器 / 四层架构（见 CODE_GRAPH）

**能力栈**
- LLM：auto 回退链 sensenova(glm-5.2) → zhipu → xunfei → baidu，支持用户级 API Key 隔离
- 语音：前端唯一 MiMo Cloud TTS；后端保留 CosyVoice→GPT-SoVITS→Bert-VITS2→Edge 四级容灾降级链（不暴露给用户）
- 记忆：shisi 双轨适配层（use_shisi_memory=true），legacy 仅表历史迁移
- 知识：RAGEngineV2 + BM25 混合检索 + 角色知识库 + 火爬虫网络人设增强（角色设置页入口）
- 工具：天气/搜索/日历/计算器/提醒/时间感知/角色卡查询
- 安全：JWT + X-API-Key 双认证、输入输出过滤、PII 匿名化、提示注入检测

**视觉语言**（glass 结构层，已生效）
- LightOnly 浅色主题 + Tailwind 单一样式体系 + 白色毛玻璃卡片 + max-w 居中工作区
- 扁平导航（三级侧边栏方案已被否决并废弃）
- React 19 + Vite 8 + Zustand 5 + React Query

**部署形态**
- Windows 开发机一键打包（deploy_ai_girlfriend.bat/ps1 双版本行为一致）
- Linux 生产：Nginx(:443) → uvicorn×4(127.0.0.1:8000) → PostgreSQL；服务器不联外网，代码 scp 上传
- OBS-1 已修复（后端仅绑 127.0.0.1）；OBS-2 HTTPS 待域名；OBS-3 服务 root 运行待降权

### 🅿️ B. 候选池（方案成熟但未获批准，冻结中）

> 以下均有完整决策链与规格文档，2026-08-26 用户批复**不予实施**。启动需逐项重新批准。

| 编号 | 候选事项 | 成熟度 |
|------|----------|--------|
| SP-1 | 状态中心丰富化（4统计+7日情绪折线+成就+趋势，framework-v1 ∪ v10 并集） | 方案齐备 |
| SP-2 | 创建角色页方案 A（横向分段 + 右侧常驻预览卡） | 规格定稿 114 行 |
| SP-3 | Demo 页删除（前端 DemoPage + demo_routes×4 后端端点） | 清单明确 |
| SP-4 | 知识库入口升级至侧边栏一级 | 方向确定 |
| SP-5 | 视觉动效层补课（gradient-shift 背景/stagger 入场/视差/CountUp + 恢复三动画组件） | 任务分解完毕 |
| SP-6~8 | 拟人化 v2 未做三模块：WorldInfoScanner 动态世界书注入 / SmartCompressor 两级上下文压缩 / EmotionPredictor 情感累积预测 | 设计完成 |

### ❌ C. 明确不做（Non-Goals，防止漂移）

| 不做的事 | 依据 |
|----------|------|
| 三层级侧边栏导航（任何形式复活） | 用户 08-24 裁决 + 架构已扁平化 |
| 前端暴露多语音引擎选项 | reinit BREAKING 决策（07-27） |
| LoRA 本地训练管线 | 07-30 移除，改为本地提取→上传→服务器分析 |
| 多模态扩展（Live2D/虚拟形象/图片生成） | 拟人化评审 §八 边界声明 |
| 任何 LLM 微调/训练（仅推理 API） | 同上 |
| 违反微信 ToS 的自动化手段 | 宪法硬约束 |
| 模型身份伪装/别名映射 | 宪法硬约束（LLM 透明） |
| shisi/ 目录废弃或重命名 | CODE_GRAPH §14 |
| 跨用户数据共享（记忆/角色/剧情串扰） | 宪法硬约束 L3 |

---

## 三、终极架构快照（一图流）

```
微信用户 ──扫码──▶ wechat_direct（指数退避重连 + 全链路结构化日志）
                        │
                        ▼
              orchestrator（fast 模式 12 步精简流程）
       ┌──────────┬──────────┬──────────┬──────────┐
       ▼          ▼          ▼          ▼          ▼
   情感引擎    三层记忆    RAG+BM25    工具集     ASE主动
 (状态机+矩阵) (shisi双轨) (知识库+火爬虫) (8内置)  (日8条/30min)
       └──────────┴──────────┴─────┬────┴──────────┘
                                   ▼
                  MultiProviderGateway（auto 四级回退）
                                   ▼
                    MiMo TTS（前端唯一，后端四级容灾）
```

## 四、成功判据（终态的自检问题）

1. 新用户从扫码到收到第一句「十四」的主动问候，全程无文档可独立完成？
2. 两个用户同时在线，记忆/情感/角色零串扰？（L3 红线）
3. 任一 LLM 供应商挂掉，对话不中断？（回退链自动切换）
4. MiMo 云故障时语音仍可用？（四级降级链）
5. 1104 测试全绿 + 新功能附测试？（质量法律）
6. 角色行为像「人」而不像「客服」？（情感状态机 + 风格矩阵生效的主观验收）

---

*本宣言是治理体系的合成的顶点：账本记录「怎么走到今天」，VISION 回答「最终是什么样」。两者冲突时，以本文件 + CODE_GRAPH 代码实况为准，按 AGENTS §1.3 真值裁决优先级仲裁。*
