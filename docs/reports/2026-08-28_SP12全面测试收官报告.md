# SP-12 全面测试收官报告

> 日期：2026-08-28 | 性质：SP-12 交付（回归门确立，此后每个功能批次以本报告四项为验收门槛）

## 结果总览（全绿）

| 验收项 | 结果 | 证据 |
|--------|------|------|
| 后端全量 | **1015 passed + 1 skipped**（98s） | `python -m pytest -q` 实跑 |
| 前端单测 | **59 passed / 11 files** | `npx vitest run` 实跑 |
| Playwright E2E 冒烟 | **5 passed / 10.7s**（登录 UI→跳转 /wechat、角色网格、LLM 设置页 API 集成、**聊天主链路 LLM 真实调用**、健康探针） | `npx playwright test` |
| 前后端对齐复跑 | **缺失 0**（前端调用缺后端必须为 0 ✅）；后端 200 端点，前端消费 121 | 对齐报告已更新二次复跑标注 |

## E2E 基建（可复用资产）

- `scripts/e2e_setup.py`：独立 E2E 数据库（data/e2e_users.db）+ 种子 admin（e2e-admin@test.local / E2eTest#2026），**零污染**本地/生产数据
- `frontend/e2e/smoke.spec.ts`：5 条冒烟（登录 UI / 角色网格 / LLM 设置集成 / 聊天 API 主链路 / 健康探针）；凭证环境变量注入
- 运行手册：`python scripts/e2e_setup.py` → `APP_DATABASE_URL="sqlite+aiosqlite:///data/e2e_users.db" API_KEY_ENABLED=false python -m uvicorn api.run_api:app --port 8000` → `cd frontend && npx playwright test`
- 修正记录：提交按钮文案实为「登 录」（带空格），初版 spec 用「登录」匹配失败——E2E 首跑即抓出 UI 文案与直觉不符，正是冒烟价值

## 回归门（此后验收标准）

新功能批次合并前必须四项全绿：pytest 全量 + vitest 全量 + E2E 冒烟 + 对齐复跑缺失=0。任一红即阻塞。

## 遗留

- E2E 暂未入 CI（本地手跑）；后续可加 GitHub Actions job（webServer 配置已支持 CI 模式 preview）
- data/e2e_users.db / test-results/ / playwright-report/ 建议补 .gitignore

---
*SP-12 完成于 2026-08-28；队列剩 SP-11（产品介绍页）*
